"""
test_calendar.py — Nyquist gate: EXE-04

Tests for B3 continuous contract resolution (WIN%→WINFyy, etc.).
Implemented in later plan (calendar/b3_calendar.py wave).
"""
import pytest


@pytest.mark.xfail(reason="implemented in later wave")
def test_contract_resolution():
    """EXE-04: WIN%/WDO%/BIT% resolve to the correct current expiry contract code."""
    raise NotImplementedError
