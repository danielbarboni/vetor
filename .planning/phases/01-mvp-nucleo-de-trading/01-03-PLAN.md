---
phase: 01-mvp-nucleo-de-trading
plan: 03
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/main.py
  - backend/config.py
  - backend/requirements.txt
  - backend/pytest.ini
  - backend/.env.example
  - backend/auth/jwt_guard.py
  - backend/db/supabase_client.py
  - backend/db/models.py
  - backend/tests/conftest.py
  - backend/tests/test_auth.py
  - backend/tests/test_robots.py
  - backend/tests/test_params.py
  - backend/tests/test_engine.py
  - backend/tests/test_calendar.py
  - backend/tests/test_orders.py
  - backend/tests/test_sumario.py
  - backend/tests/test_backtest.py
  - backend/tests/test_broker.py
  - .github/workflows/ci.yml
autonomous: false
requirements: []
must_haves:
  truths:
    - "FastAPI app starts, exposes /health returning 200, and rejects requests with missing/invalid JWT"
    - "get_current_user dependency validates Supabase JWT and yields the authenticated user_id"
    - "pytest harness runs with fixtures for a test supabase client and a mock MetaAPI adapter"
    - "CI runs frontend lint+typecheck+vitest and backend pytest on push"
  artifacts:
    - path: "backend/auth/jwt_guard.py"
      provides: "get_current_user FastAPI dependency validating Supabase JWT"
      contains: "audience"
    - path: "backend/main.py"
      provides: "FastAPI app with CORS, health check, router includes"
      contains: "FastAPI"
    - path: "backend/tests/conftest.py"
      provides: "pytest fixtures: test supabase client + mock MetaAPI adapter"
      min_lines: 20
    - path: ".github/workflows/ci.yml"
      provides: "CI: frontend + backend gates"
      contains: "pytest"
  key_links:
    - from: "backend/main.py"
      to: "backend/auth/jwt_guard.py"
      via: "Depends(get_current_user) on protected routers"
      pattern: "get_current_user"
    - from: "backend/auth/jwt_guard.py"
      to: "SUPABASE_JWT_SECRET"
      via: "jwt.decode with audience=authenticated"
      pattern: "decode"
---

<objective>
Scaffold the FastAPI backend under `/backend`: app entry with CORS allowlist, health check, the Supabase JWT guard dependency, the single Supabase client, Pydantic base models, and the complete pytest test harness (conftest + empty test modules mapped to Phase 1 requirements per the Nyquist validation map). Add the CI workflow. Verify the [ASSUMED] packages before install.

Purpose: This is the backend foundation every API and engine plan depends on. The JWT guard is the API trust boundary (security V2/V3). The test harness scaffolds the Nyquist gate files so later plans have `<automated>` targets that exist (no "MISSING" gates). CI enforces quality on every push.
Output: A running FastAPI app at `localhost:8000` with /health, a working JWT guard, a green (empty) pytest harness, and CI configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md

<interfaces>
<!-- Stack + patterns locked by RESEARCH.md. -->
Backend deps (RESEARCH Standard Stack): fastapi@0.137.1, uvicorn[standard]@0.49.0, pydantic@2.13.4,
supabase@2.31.0 (python), python-jose[cryptography]@~3.3 [ASSUMED], python-multipart [ASSUMED],
httpx@~0.x [ASSUMED], APScheduler@~3.10 [ASSUMED]. Dev: pytest, pytest-asyncio, httpx.

JWT guard pattern (RESEARCH Pattern 1): HTTPBearer; jwt.decode(token, SUPABASE_JWT_SECRET,
audience="authenticated"); raise 401 on JWTError. get_current_user returns the validated user_id (sub claim).

CORS (D-claude discretion + RESEARCH security): allowlist Cloudflare Pages domain + http://localhost:5173 only.
API versioning prefix: planner choice — use no prefix for Phase 1 (endpoints flat per RESEARCH API Surface).

Nyquist test map (RESEARCH Validation Architecture): each test_*.py corresponds to a requirement group.
conftest must provide: test supabase client fixture + mock MetaAPI adapter fixture (RESEARCH Wave 0 Gaps).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
    The backend requires two [ASSUMED] PyPI packages flagged by RESEARCH (slopcheck was unavailable): python-jose[cryptography] and APScheduler. Per the package legitimacy gate these must be human-verified before the first pip install.
  </what-built>
  <how-to-verify>
    1. Open https://pypi.org/project/python-jose/ — confirm it is the maintained JWT library (mpdavis/python-jose), recent releases, not a typosquat.
    2. Open https://pypi.org/project/APScheduler/ — confirm it is the Advanced Python Scheduler project, widely used, recent releases.
    3. Confirm metaapi-cloud-sdk at https://pypi.org/project/metaapi-cloud-sdk/ is the official MetaAPI SDK and 29.1.1 is current.
  </how-to-verify>
  <resume-signal>Type "approved" to authorize installing python-jose, APScheduler, and metaapi-cloud-sdk (or name any package to reject).</resume-signal>
</task>

<task type="auto">
  <name>Task 1: Scaffold FastAPI app, config, CORS, JWT guard, Supabase client</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Recommended Project Structure backend/; Pattern 1 JWT guard; Security Domain; Environment Availability)
    - .planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md (D-15 local dev uvicorn:8000; D-16 deployment; claude discretion CORS/env/versioning)
  </read_first>
  <action>
    Create `backend/requirements.txt` pinning: fastapi==0.137.1, uvicorn[standard]==0.49.0, pydantic==2.13.4, supabase==2.31.0, python-jose[cryptography], python-multipart, httpx, APScheduler, metaapi-cloud-sdk==29.1.1; dev: pytest, pytest-asyncio. Install into a venv.
    Create `backend/config.py` — a pydantic-settings Settings class reading: SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_KEY, SUPABASE_JWT_SECRET, METAAPI_TOKEN, METAAPI_SYSTEM_ACCOUNT_ID, CORS_ORIGINS (comma list), ENV. Create `backend/.env.example` mirroring these keys.
    Create `backend/db/supabase_client.py` — a single supabase client built from SUPABASE_URL + SUPABASE_SERVICE_KEY (server-side service role), exported as `supabase`.
    Create `backend/auth/jwt_guard.py` implementing RESEARCH Pattern 1: HTTPBearer security; `get_current_user(credentials) -> str` that decodes the token with SUPABASE_JWT_SECRET, audience="authenticated", returns the `sub` (user_id), raises HTTPException(401) on JWTError or missing token. NEVER trust a client-supplied user_id — always use the value from the decoded token (mitigates T-01-04).
    Create `backend/db/models.py` — Pydantic v2 base models matching the schema: RobotCreate, RobotOut, OrderOut, BacktestCreate, BacktestOut, ProfileOut, PreferencesOut, BrokerConnectionOut. Mirror the column types/enums from RESEARCH Data Model.
    Create `backend/main.py` — FastAPI app; add CORSMiddleware with allowlist from settings.CORS_ORIGINS (localhost:5173 + Cloudflare domain) NOT wildcard; `GET /health` returns {"status":"ok"}; placeholder router includes (commented stubs for auth, robots, execution, sumario, backtests, conta) to be filled by later plans.
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/backend && python -c "import main" 2>&1 | tail -3 && grep -q "audience" auth/jwt_guard.py && grep -q "CORSMiddleware" main.py && grep -qv "allow_origins=\[\"\\*\"\]" main.py && echo BACKEND_OK</automated>
  </verify>
  <acceptance_criteria>
    - `backend/main.py` imports without error and defines GET /health
    - CORSMiddleware configured with an explicit allowlist (not "*")
    - `backend/auth/jwt_guard.py` decodes with audience="authenticated" and returns user_id from sub claim
    - `backend/db/supabase_client.py` exports a single client
    - `backend/.env.example` lists SUPABASE_JWT_SECRET and METAAPI_TOKEN
  </acceptance_criteria>
  <done>FastAPI app boots with /health, CORS allowlist, JWT guard, single Supabase client, base Pydantic models.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold pytest harness (conftest + Nyquist test modules) and CI</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Validation Architecture — Test Framework, Phase Requirements → Test Map, Wave 0 Gaps)
  </read_first>
  <action>
    Create `backend/pytest.ini` (asyncio_mode=auto, testpaths=tests, addopts="-x -q --timeout=10" — add pytest-timeout to requirements if used).
    Create `backend/tests/conftest.py` with fixtures from RESEARCH Wave 0 Gaps: `test_supabase` (a client pointed at a test schema/project via env, or a lightweight fake for unit tests), `mock_metaapi` (a MagicMock/AsyncMock mimicking the MetaAPIAdapter surface: connect, subscribe, place_order, get_positions, get_orders), and `auth_token` (a signed test JWT for protected-route tests).
    Create the Nyquist test module skeletons (one file per RESEARCH test map group), each with at least one xfail/placeholder test named exactly per the map so later plans fill them in: test_auth.py (test_register, test_login, test_reset, test_oauth_url), test_robots.py (test_name_unique, test_create_robot), test_params.py (test_it_validation), test_engine.py (test_start_requires_params), test_calendar.py (test_contract_resolution), test_orders.py (test_order_persist, test_idempotency), test_sumario.py (test_equity_calc), test_backtest.py (test_metrics_parity), test_broker.py (test_provision). Mark each placeholder `@pytest.mark.xfail(reason="implemented in later wave")` so the suite is green now.
    Create `.github/workflows/ci.yml` as the MERGE GATE for `main` (triggers: `push` to any branch + `pull_request` targeting main; add a `concurrency` group keyed on ref to cancel superseded runs). Two required jobs with explicit named steps so they can be set as branch-protection required status checks:
    - job `frontend` (node 24): `cd frontend && npm ci` → `npm run lint` (eslint) → `npm run typecheck` (tsc --noEmit) → `npm run build` → `npm run test` (vitest run).
    - job `backend` (python 3.12): `cd backend && pip install -r requirements.txt ruff` → `ruff check .` → `pytest`.
    Use dummy/test env vars for jobs that don't need live Supabase; mark integration tests that require Supabase/MetaAPI to skip when env absent (never put real secrets in CI). The job names `frontend` and `backend` are the contract for the branch-protection required checks on main (see docs/DEVELOPMENT_WORKFLOW.md).
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/backend && pytest 2>&1 | tail -5 && grep -q "mock_metaapi" tests/conftest.py && test -f ../.github/workflows/ci.yml && grep -q "pytest" ../.github/workflows/ci.yml && echo HARNESS_OK</automated>
  </verify>
  <acceptance_criteria>
    - `pytest` exits 0 (placeholders xfail, harness green)
    - `tests/conftest.py` defines test_supabase, mock_metaapi, auth_token fixtures
    - All 10 test_*.py modules from the RESEARCH test map exist with the named test functions
    - `.github/workflows/ci.yml` defines `frontend` (lint+typecheck+build+vitest) and `backend` (ruff+pytest) jobs, triggered on push + pull_request, as the merge gate for main
  </acceptance_criteria>
  <done>pytest harness green with all Nyquist module/function names scaffolded; CI runs both stacks (lint+typecheck+build+test FE, ruff+pytest BE) as the named merge gate for main.</done>
</task>

</tasks>

<verification>
- `cd backend && python -c "import main"` succeeds; `/health` returns 200
- `cd backend && pytest` exits 0
- JWT guard rejects missing/invalid tokens (audience-checked)
- CI workflow present and runs both frontend and backend gates
</verification>

<success_criteria>
FastAPI backend scaffolded with CORS allowlist, Supabase JWT guard, single Supabase client, base Pydantic models, a green pytest harness covering all Nyquist test names, and CI configured. [ASSUMED] packages human-verified before install.
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-03-SUMMARY.md` when done.
</output>
