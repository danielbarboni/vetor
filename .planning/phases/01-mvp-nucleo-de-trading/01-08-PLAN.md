---
phase: 01-mvp-nucleo-de-trading
plan: 08
type: execute
wave: 4
depends_on: ["01-05", "01-06", "01-07"]
files_modified:
  - backend/strategies/it_params_schema.py
  - backend/strategies/it_validators.py
  - backend/routers/robots.py
  - backend/tests/test_params.py
  - frontend/src/pages/robots/RobotEditor.tsx
  - frontend/src/components/editor/EditorShell.tsx
  - frontend/src/components/editor/ActionRail.tsx
  - frontend/src/components/editor/ParameterSection.tsx
  - frontend/src/components/editor/IndicadorRow.tsx
  - frontend/src/components/editor/sections/IndicadoresTecnicos.tsx
  - frontend/src/data/indicatorDefs.ts
autonomous: true
requirements: ["EDT-01", "EDT-02", "EDT-03", "EDT-04"]
must_haves:
  truths:
    - "The IT [Tangram 3.0] editor renders all 14 indicators with their fields plus the 11 ordered sections (EDT-01)"
    - "Parameters are read-only with a lock banner while the robot is executando (EDT-02)"
    - "Saving validates JSON schema + cross-field rules and records params_saved_at (EDT-03)"
    - "A backtest shortcut icon in the editor header opens the backtest modal (EDT-04)"
  artifacts:
    - path: "backend/strategies/it_params_schema.py"
      provides: "Pydantic v2 model for the full IT param schema"
      contains: "model_validator"
    - path: "frontend/src/data/indicatorDefs.ts"
      provides: "Definitions for all 14 indicators (fields, options)"
      contains: "Médias Móveis"
    - path: "frontend/src/pages/robots/RobotEditor.tsx"
      provides: "Editor screen with section accordion + action rail"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/robots/RobotEditor.tsx"
      to: "PATCH /robots/{id}"
      via: "api.updateRobot saves params"
      pattern: "updateRobot|params"
    - from: "backend/routers/robots.py"
      to: "backend/strategies/it_validators.py"
      via: "validate params before persisting on PATCH"
      pattern: "validate"
---

<objective>
Build the Indicadores Técnicos [Tangram 3.0] parameter editor (EDT-01): the full editor shell (SUMÁRIO/GRÁFICO/PARÂMETROS tabs, fixed action rail), the 11 ordered parameter sections, all 14 indicators with their exact fields, the execution-lock read-only mode (EDT-02), backend cross-field validation on save (EDT-03), and the backtest shortcut icon (EDT-04). This is the most detail-dense feature in the phase.

Purpose: The editor is the only configurable strategy in Phase 1 (D-10). Correct cross-field validation (RISK-04) is safety-critical — invalid params must not reach live execution. PRD §12 is authoritative for indicator fields.
Output: A working editor at /robos/{id}/parametros with validated save.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md

<interfaces>
From plan 01: UI primitives (Input, Toggle, SegmentControl, Button, Badge, Modal), Robot type.
From plan 05/06: api.getRobot, api.updateRobot (PATCH /robots/{id}); backend robots router + robot_repo.
From plan 03: get_current_user, Pydantic base.

PRD §12 is authoritative (RESEARCH RISK-04, Assumption A8): read §12.3 (general entry params),
§12.4 (the 14-indicator catalog — each indicator's fields + dropdown options + toggles), §12.5 (section order).
The 14 indicators: 1 Médias Móveis, 2 Cruzamento de 3 Médias, 3 HiLo Activator, 4 MACD, 5 ADX DI+/DI−,
6 Estocástico, 7 VWAP, 8 IFR(RSI), 9 Bandas de Bollinger, 10 Stop ATR, 11 SAR Parabólico, 12 OBV,
13 Detector de Topos e Fundos, 14 Pontos Pivot. Common fields per indicator: Habilitar Inversão (toggle),
Modo de Operação (dropdown), Forma de uso (dropdown — options vary). Several indicators have value-filter toggles
that reveal extra fields (MACD filtro de valor, ADX min/max + Aumento/Diminuição, etc.).
Section order (§12.5): Mercado, Gráfico, Indicadores Técnicos, Filtros de Entrada, Aumento de Posição,
Critérios de Saída, Critérios de Saída Diário, Gerenciamento de Capital, Horário de Operação, Módulo Day Trade, Informações.

UI-SPEC Editor Shell: tabs (.et/.et-on SUMÁRIO/GRÁFICO/PARÂMETROS), fixed action rail (44x44 icon buttons:
play/stop, save 💾, backtest 🧪 --color-primary, costs), parameter sections (numbered badge, accordion),
param row grid 250px/1fr, execution lock banner (--tint-amber, exact copy "Robô em execução: não é possível...").
Copy: "ÚLTIMO SALVAR: [DD/MM/YYYY HH:MM]", tabs, icon tooltips "Salvar parâmetros"/"Criar backtest", section titles.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend IT param schema + cross-field validators + validated save</name>
  <read_first>
    - PRD_Plataforma_Robos_Trading_v2.0.md §12.3, §12.4 (all 14 indicators + fields + filter toggles), §12.5 (section order); §18 RF-EXE-01 (save with validation)
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (RISK-04 cross-field rules, A8; Pitfall — Pydantic model_validator)
    - backend/routers/robots.py + backend/db/robot_repo.py (plan 05 PATCH)
    - backend/db/models.py (plan 03)
  </read_first>
  <behavior>
    - a fully valid IT param payload validates and persists, setting params_saved_at
    - enabling an indicator with a missing required field (e.g. Médias Móveis without período) fails validation
    - a cross-field rule violation (e.g. MACD filtro de valor toggled on but no Valor do filtro) fails validation
    - at least one indicator must be enabled, else validation fails with a clear message
  </behavior>
  <action>
    Create `backend/strategies/it_params_schema.py` — Pydantic v2 models for the full IT [Tangram 3.0] schema mirroring PRD §12: per-indicator submodels (all 14) with their exact fields and option enums from §12.4, the general entry params (§12.3), and section containers for the 11 sections (§12.5). Use Optional fields revealed by filter toggles.
    Create `backend/strategies/it_validators.py` — cross-field validators using `@model_validator(mode='after')` enumerating every cross-field rule found in PRD §12 (each indicator's required-when-enabled fields; filter-toggle-reveals-field rules for MACD/ADX/OBV etc.; "at least one indicator enabled"; numeric positivity rules like OBV "Candles no Mesmo Sentido" positive). If PRD §12 leaves a rule implicit, encode the prototype-observed behavior and add a `# RISK-04` comment flagging it for human review.
    Wire validation into PATCH /robots/{id} in routers/robots.py: when the body includes params, validate via the IT schema before persisting; on failure return 422 with field errors; on success set params_saved_at=now() (EDT-03 / RF-EXE-01).
    Write pytest tests in test_params.py::test_it_validation (one assertion per cross-field rule group covering the four behaviors above).
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/backend && pytest tests/test_params.py::test_it_validation 2>&1 | tail -6 && grep -q "model_validator" strategies/it_params_schema.py && grep -qi "validate" routers/robots.py && echo IT_VALIDATION_OK</automated>
  </verify>
  <acceptance_criteria>
    - test_params.py::test_it_validation passes with per-rule assertions
    - it_params_schema.py models all 14 indicators + general params + 11 sections
    - PATCH /robots/{id} validates params and sets params_saved_at on success (EDT-03)
    - "at least one indicator enabled" and filter-toggle reveal rules are enforced
  </acceptance_criteria>
  <done>Full IT param schema + cross-field validators enforced on save, tested per rule.</done>
</task>

<task type="auto">
  <name>Task 2: Editor shell, action rail, parameter sections, indicator defs</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Editor Shell section; Input Fields; Toggle; Segment Controls; Editor copy table)
    - PRD_Plataforma_Robos_Trading_v2.0.md §12.4 (indicator fields), §12.5 (section order)
    - frontend/src/lib/api.ts (api.getRobot/updateRobot), frontend/src/components/ui/
  </read_first>
  <action>
    Create `frontend/src/data/indicatorDefs.ts` — declarative definitions for all 14 indicators: {id, name, commonFields, formaDeUsoOptions, specificFields (with type number/dropdown/toggle/decimal and options), conditionalReveals}. Mirror PRD §12.4 exactly (names in PT-BR: Médias Móveis, Cruzamento de 3 Médias Móveis, HiLo Activator, MACD, ADX — DI+/DI−, Estocástico (Pleno), VWAP, IFR (RSI), Bandas de Bollinger, Stop ATR, SAR Parabólico, OBV, Detector de Topos e Fundos, Pontos Pivot).
    Create `frontend/src/components/editor/EditorShell.tsx` — header bar with breadcrumb "Robôs", robot name + rename, "ÚLTIMO SALVAR: ..." timestamp, tabs (.et/.et-on) SUMÁRIO/GRÁFICO/PARÂMETROS. For Phase 1 PARÂMETROS is the active editor; SUMÁRIO tab links to the sumário screen (plan 11); GRÁFICO is a Phase 2 placeholder.
    Create `frontend/src/components/editor/ActionRail.tsx` — fixed right rail (44x44 icon buttons): play/stop (state-aware, calls api.startRobot/stopRobot), save 💾 (tooltip "Salvar parâmetros"), backtest 🧪 (--color-primary, tooltip "Criar backtest" — opens the backtest modal from plan 12, EDT-04), costs.
    Create `frontend/src/components/editor/ParameterSection.tsx` — accordion section (numbered badge, title, subtitle, collapsible body) and `IndicadorRow.tsx` — a single indicator block (ON/OFF toggle revealing common + specific fields driven by indicatorDefs, with conditional reveals for filter toggles).
    Create `frontend/src/components/editor/sections/IndicadoresTecnicos.tsx` — renders the general entry params + all 14 IndicadorRow blocks.
    Create `frontend/src/pages/robots/RobotEditor.tsx` (route /robos/:id/parametros): loads the robot via api.getRobot, renders EditorShell + the 11 ordered sections (§12.5) — Indicadores Técnicos fully built; the other sections rendered with their fields per PRD §12.5 (Mercado, Gráfico, Filtros de Entrada, Aumento de Posição, Critérios de Saída, Critérios de Saída Diário, Gerenciamento de Capital, Horário de Operação, Módulo Day Trade, Informações). Save (rail or section) collects params and calls api.updateRobot; on 422 show field errors; on success update the timestamp. EDT-02: when robot.status==='executando' render the execution lock banner (exact copy) and make all fields read-only.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -q "Médias Móveis" src/data/indicatorDefs.ts && grep -c "name:" src/data/indicatorDefs.ts && grep -q "PARÂMETROS" src/components/editor/EditorShell.tsx && grep -q "Robô em execução" src/pages/robots/RobotEditor.tsx && grep -qE "updateRobot|params" src/pages/robots/RobotEditor.tsx && echo EDITOR_OK</automated>
  </verify>
  <acceptance_criteria>
    - indicatorDefs.ts defines all 14 indicators (grep "name:" count >= 14) starting with "Médias Móveis"
    - RobotEditor renders the 11 sections in PRD §12.5 order with Indicadores Técnicos fully built (EDT-01)
    - Execution lock banner with exact copy + read-only fields when status executando (EDT-02)
    - Save calls api.updateRobot with params; action rail has backtest 🧪 icon (EDT-04)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Full IT editor renders 14 indicators + 11 sections, lock mode, validated save, backtest shortcut.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/test_params.py` exits 0
- `cd frontend && npm run build` exits 0
- Editor shows all 14 indicators + 11 sections; save validates; lock banner appears when executing
</verification>

<success_criteria>
IT [Tangram 3.0] editor complete: full 14-indicator editor in §12.5 section order (EDT-01), execution lock (EDT-02), validated save with timestamp (EDT-03), backtest shortcut (EDT-04).
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-08-SUMMARY.md` when done.
</output>
