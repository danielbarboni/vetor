---
phase: 01-mvp-nucleo-de-trading
plan: 09
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - backend/broker/poc_metaapi.py
  - docs/metaapi-poc-findings.md
autonomous: false
requirements: ["EXE-04"]
must_haves:
  truths:
    - "The MetaAPI v29 Python SDK connects to the system account and receives ticks for at least one B3 instrument"
    - "The actual v29 API surface (method names, event shapes, async model) is documented before the full adapter is built"
  artifacts:
    - path: "backend/broker/poc_metaapi.py"
      provides: "Throwaway PoC script: connect, subscribe, receive ticks, place a simulated order"
      min_lines: 30
    - path: "docs/metaapi-poc-findings.md"
      provides: "Documented v29 API surface + tick schema + gotchas"
      contains: "tick"
  key_links:
    - from: "backend/broker/poc_metaapi.py"
      to: "MetaAPI v29 SDK"
      via: "MetaApi client connect + subscribe"
      pattern: "MetaApi|metaapi"
---

<objective>
De-risk the MetaAPI v29 Python SDK BEFORE building the full broker adapter (RISK-01, A2, A9). Write a proof-of-concept that connects to the system MetaAPI account, subscribes to one B3 instrument's tick stream, verifies tick events arrive with the expected schema, attempts a simulated order, and documents the actual v29 API surface. The full adapter (plan 10) is written against these findings, not training-data assumptions.

Purpose: The SDK jumped v21→v27→v29 — large gaps imply breaking changes. Failing fast here prevents building the entire engine layer against a wrong API. This is the riskiest external integration in the phase.
Output: A findings doc that plan 10's BrokerPort/MetaAPIAdapter implements against.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md

<interfaces>
From plan 03: backend/config.py (METAAPI_TOKEN, METAAPI_SYSTEM_ACCOUNT_ID), .env.
Package: metaapi-cloud-sdk==29.1.1 (human-verified in plan 03).
RESEARCH RISK-01/02, A2/A9: verify (1) connect/auth flow, (2) tick subscription + event shape,
(3) async model vs FastAPI event loop (native asyncio vs thread bridge), (4) reconnection behavior
(SDK does NOT replay missed ticks — RISK-02), (5) order placement + clientOrderId/comment support,
(6) get_positions()/get_orders() for state rehydration (EXE-06).
D-06: system MetaAPI account (Vetor platform) is used for Simulado tick data.
B3 instruments: WIN%, WDO%, BIT% resolve to current expiry (EXE-04 — the PoC subscribes to one resolved symbol).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the MetaAPI v29 proof-of-concept script</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Standard Stack metaapi note; RISK-01, RISK-02; Integration Touchpoint 2; Architectural Responsibility Map broker tier)
    - backend/config.py (plan 03 METAAPI_* settings)
  </read_first>
  <action>
    Create `backend/broker/poc_metaapi.py` — a standalone async script (run via `python -m backend.broker.poc_metaapi`) that, using metaapi-cloud-sdk v29 and METAAPI_TOKEN + METAAPI_SYSTEM_ACCOUNT_ID from config: (1) connects to the system account and waits for synchronization; (2) resolves and subscribes to one B3 instrument's tick stream (e.g. the current WIN front-month symbol); (3) prints the first ~20 tick events with their full structure (fields, types, timestamp format); (4) attempts a small simulated/market order with a deterministic comment/clientId and prints the response/event; (5) calls get_positions() and get_orders() and prints their shapes; (6) registers a reconnection/synchronization listener and logs what happens on disconnect (RISK-02). Use try/except around each step and clear logging so a credentialed human run produces a readable transcript. Reference the official MetaAPI v29 docs via Context7 (resolve-library-id metaapi-cloud-sdk, query connection + streaming) — do NOT guess the API from training data.
    The script must be clearly marked throwaway (header comment) — plan 10 reimplements properly behind BrokerPort.
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/.claude/worktrees/phase-1-context/backend && python -c "import ast; ast.parse(open('broker/poc_metaapi.py').read())" && grep -qiE "MetaApi|metaapi" broker/poc_metaapi.py && grep -qiE "subscribe|on_symbol_price|tick" broker/poc_metaapi.py && echo POC_SCRIPT_OK</automated>
  </verify>
  <acceptance_criteria>
    - poc_metaapi.py parses as valid Python and imports the v29 SDK
    - Script covers: connect, subscribe to ticks, place order, get_positions/get_orders, reconnection listener
    - Credentials read from config (no hardcoded secrets)
  </acceptance_criteria>
  <done>Throwaway PoC script written against the real v29 SDK surface, ready for a credentialed run.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    The MetaAPI PoC script is ready. It requires a real MetaAPI account token + system account ID to run (Claude cannot execute it without live credentials). The run produces the ground-truth v29 API surface that the full broker adapter (plan 10) depends on.
  </what-built>
  <how-to-verify>
    1. Put METAAPI_TOKEN and METAAPI_SYSTEM_ACCOUNT_ID into backend/.env (provision the Vetor system MetaAPI account if not done).
    2. Run `python -m backend.broker.poc_metaapi` from the backend venv.
    3. Confirm: connection synchronizes; tick events arrive for the subscribed B3 instrument with a readable schema; a simulated order returns a response; get_positions/get_orders return; the reconnection listener logs on a forced disconnect.
    4. Record the observed API surface in `docs/metaapi-poc-findings.md`: exact class/method names, tick event field names+types, async model (native asyncio vs thread bridge — confirms/refutes A9), order placement signature + clientOrderId/comment support, get_positions/get_orders shapes, and reconnection behavior (confirm RISK-02: missed ticks NOT replayed). Note any deviations from RESEARCH assumptions.
  </how-to-verify>
  <resume-signal>Type "approved" once the PoC runs successfully and docs/metaapi-poc-findings.md documents the v29 surface (or describe failures — plan 10 will adapt).</resume-signal>
</task>

</tasks>

<verification>
- poc_metaapi.py parses and targets the v29 SDK
- docs/metaapi-poc-findings.md exists and documents tick schema + API surface (checkpoint)
- Reconnection behavior (RISK-02) explicitly confirmed
</verification>

<success_criteria>
MetaAPI v29 SDK proven against a live system account: ticks received, order placed, state queries work, reconnection behavior understood — all documented for plan 10. EXE-04 contract-resolution input (current expiry symbol subscription) validated.
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-09-SUMMARY.md` when done.
</output>
