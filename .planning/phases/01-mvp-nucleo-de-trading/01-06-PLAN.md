---
phase: 01-mvp-nucleo-de-trading
plan: 06
type: execute
wave: 3
depends_on: ["01-05", "01-01", "01-04"]
files_modified:
  - frontend/src/pages/robots/RobotList.tsx
  - frontend/src/components/robot-card/RobotCard.tsx
  - frontend/src/components/robot-card/MaisInfo.tsx
  - frontend/src/components/robot-card/ContextMenu.tsx
  - frontend/src/components/charts/SparklineChart.tsx
  - frontend/src/components/charts/SparklineChart.test.tsx
  - frontend/src/components/robot-card/RobotFilters.tsx
  - frontend/src/stores/robots.ts
  - frontend/src/lib/api.ts
autonomous: true
requirements: ["ROB-01", "ROB-02", "ROB-03", "ROB-04", "ROB-06", "ROB-07"]
must_haves:
  truths:
    - "Robots are shown in three tabs EXECUTANDO / PARADOS / ARQUIVADOS (ROB-01)"
    - "Each robot renders as a card with sparkline, badges, metrics, position status (ROB-02)"
    - "Each card has MAIS INFO accordion, context menu, and state-varying control button (ROB-03)"
    - "MAIS INFO expands to trades count, profitable %, profit factor, max drawdown (ROB-04)"
    - "Filters by strategy + posicionados, grid/list toggle, and name search work (ROB-06)"
    - "CRIAR ROBÔ launches the wizard (ROB-07)"
  artifacts:
    - path: "frontend/src/components/robot-card/RobotCard.tsx"
      provides: "Pixel-faithful robot card per UI-SPEC"
      min_lines: 60
    - path: "frontend/src/components/charts/SparklineChart.tsx"
      provides: "ECharts 6.1.0 sparkline equity component"
      contains: "echarts"
    - path: "frontend/src/lib/api.ts"
      provides: "Typed REST client adding Bearer token to all requests"
      contains: "Authorization"
  key_links:
    - from: "frontend/src/pages/robots/RobotList.tsx"
      to: "/robots"
      via: "TanStack Query fetch via lib/api"
      pattern: "robots"
    - from: "frontend/src/lib/api.ts"
      to: "auth store session"
      via: "Authorization: Bearer access_token"
      pattern: "Bearer"
---

<objective>
Build the robot listing screen at `/robos` with pixel-faithful robot cards: three state tabs, ECharts 6.1.0 sparkline equity chart, simulator/status badges, primary metrics, MAIS INFO accordion, per-state context menu, control button, filters (strategy multi-select + posicionados), grid/list toggle, name search, empty states, and the CRIAR ROBÔ FAB launching the wizard. Also create the typed REST client all frontend plans use.

Purpose: This is the primary screen of the app and the entry point to the wizard, editor, and reports. The shared `lib/api.ts` Bearer-token client is consumed by every subsequent frontend plan.
Output: `/robos` fully functional against the robots API, fed by a Zustand robots store + TanStack Query.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md

<!-- CHART RENDERING RULE:
     ECharts 6.1.0 is used for all charts: sparklines (robot cards), equity curves
     (Sumário/backtest), and candlestick+volume (Gráfico screen, Phase 2).
     Use echarts@6.1.0 (pinned per Assumption A1 for prototype fidelity).
     Do NOT use inline SVG for SparklineChart or EquityChart. -->

<interfaces>
From plan 01: UI primitives (Button, Badge, SegmentControl, Input, Modal), types/index.ts (Robot).
From plan 04: auth store (session.access_token), useAuth.
From plan 05 (backend): GET /robots?status=, POST /robots/{id}/archive|unarchive|duplicate, DELETE /robots/{id}.

SparklineChart uses ECharts 6.1.0:
  props: sparkline_data: number[], positive?: boolean
  Mount via useRef + echarts.init(ref.current, null, { renderer: 'svg' })
  Container div: width=220, height=56
  chart.setOption: grid all zeros, xAxis/yAxis both hidden, series type='line', smooth=true,
  symbol='none', lineStyle color=positive ? var(--color-profit) : var(--color-loss), width=1.8,
  areaStyle opacity=0.10. Return () => chart.dispose() in useEffect cleanup.
  Empty/flat state: pass data=[0,0] or omit — ECharts renders a flat line naturally.

Card states: EXECUTANDO border --color-profit + pulse dot, control "PARAR" (--tint-loss/--color-loss);
PARADO control "INICIAR" (--tint-profit/--color-profit); ARQUIVADO control "RESTAURAR" (--surface3/--muted).
MAIS INFO grid (ROB-04): Número de trades, Trades com lucro %, Fator de lucro, Drawdown máximo.
Context menu actions vary by state (ROB-05): Ver Sumário, Editar parâmetros, Iniciar, Pausar/Parar,
Arquivar, Desarquivar, Excluir, Criar backtest, Duplicar.
Copy (UI-SPEC Robot Listing): "CRIAR ROBÔ"; tabs "EXECUTANDO"/"PARADOS"/"ARQUIVADOS"; "Buscar robô…";
"Todas as estratégias"; "Posicionados"; "MAIS INFO"; quick links "CONFIGURAR"/"DUPLICAR"/"ARQUIVAR"/
"DESARQUIVAR"/"CRIAR BACKTEST"; empty heading "Nenhum robô neste ambiente" + body + CTA per spec.
"Rascunho" is a sub-state shown inside PARADOS (CONTEXT specifics), not a 4th tab.
Destructive confirms (UI-SPEC table): Excluir robô / Arquivar robô modals — exact copy + --color-loss confirm.
</interfaces>

<prototype_fidelity>
PROTOTYPE IS THE PIXEL AUTHORITY (user decision 2026-06-16). Before building the card/list, extract the `data-screen="robos"` region from `Plataforma Vetor v3.dc.html` and port its markup + inline styles directly to React (card layout, sparkline container, badges, MAIS INFO grid, context menu, tabs, filters, FAB). Convert dc-runtime (`{{expr}}`→JSX, `sc-if`→conditional, `sc-for`→map, `onClick="{{h}}"`→handler, class-toggle→React state). UI-SPEC supplements exact values; on conflict the prototype wins.
</prototype_fidelity>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared typed API client + robots store + SparklineChart (ECharts)</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (API Surface; Bearer convention; Pattern 5 ECharts Sparkline)
    - frontend/src/stores/auth.ts (plan 04 session token)
    - frontend/src/types/index.ts (plan 01 Robot type)
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Robot Card sparkline § — viewBox 0 0 220 56; Chart Colors)
  </read_first>
  <action>
    Create `frontend/src/lib/api.ts` — a typed fetch wrapper reading VITE_API_BASE_URL; every request attaches `Authorization: Bearer ${session.access_token}` from the auth store; typed helpers: getRobots(status?), getRobot(id), createRobot, updateRobot, archiveRobot, unarchiveRobot, duplicateRobot, deleteRobot, startRobot(id), stopRobot(id) (the last two call endpoints delivered by plan 10 — define them here so handlers wire up now). Throw typed errors on non-2xx. This is the single REST entry point for all frontend plans.
    Create `frontend/src/stores/robots.ts` (Zustand) holding UI filter state: active tab, strategy filter (multi), posicionados toggle, grid/list, search. Robot data itself is fetched via TanStack Query in the page.
    Create `frontend/src/components/charts/SparklineChart.tsx` — an ECharts 6.1.0 React component. Import echarts from 'echarts'. Props: sparkline_data: number[], positive?: boolean (true when last >= first). Use useRef + useEffect: call echarts.init(ref.current, null, { renderer: 'svg' }), setOption with grid all zeros, xAxis/yAxis hidden (show:false), series type='line', smooth=true, symbol='none', lineStyle { color: positive ? 'var(--color-profit)' : 'var(--color-loss)', width: 1.8 }, areaStyle { color: positive ? 'var(--tint-primary)' : 'var(--tint-loss)', opacity: 0.10 }. Return () => chart.dispose() in cleanup. Container div width=220 height=56.
    Create `frontend/src/components/charts/SparklineChart.test.tsx` (Nyquist ROB-02): asserts the component mounts without error on non-empty sparkline_data; asserts an echarts instance is initialized on the container (check ref.current has an echarts chart attached via echarts.getInstanceByDom); asserts no crash on empty data (component renders with data=[]); asserts the echarts import exists in the module.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/charts/SparklineChart.test.tsx 2>&1 | tail -5 && grep -q "Bearer" src/lib/api.ts && grep -q "echarts" src/components/charts/SparklineChart.tsx && echo API_SPARK_OK</automated>
  </verify>
  <acceptance_criteria>
    - SparklineChart.test.tsx passes (ECharts instance initializes on data, no crash on empty)
    - lib/api.ts attaches Authorization: Bearer from the auth session
    - SparklineChart.tsx imports echarts and uses echarts.init (not raw SVG polyline)
    - Container div is 220x56; lineStyle width=1.8; areaStyle opacity=0.10
  </acceptance_criteria>
  <done>Shared Bearer-auth API client, robots UI store, and tested ECharts sparkline exist.</done>
</task>

<task type="auto">
  <name>Task 2: RobotCard + MAIS INFO + context menu + control button</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Robot Card section — internal layout, control button by state, FAB, badges; Status Badge Color Map; Buttons; Modals; Destructive Confirmations)
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Pitfall 5 — no dc-runtime toggle logic)
    - frontend/src/components/charts/SparklineChart.tsx (Task 1)
    - frontend/src/components/ui/ (Badge, Button, Modal)
  </read_first>
  <action>
    Create `frontend/src/components/robot-card/RobotCard.tsx` following the UI-SPEC Robot Card internal layout top-to-bottom EXACTLY: (1) meta row — #id JBMono 10.5px, asset JBMono 13px w700, simulator badge when mode=simulado, ASSINADA badge optional, spacer, pulse dot 8x8 only on EXECUTANDO, ⋮ menu; (2) name Sora 15.5px w600 + strategy Inter 12px --muted; (3) SparklineChart (ECharts); (4) position status dot + text (COMPRADO/VENDIDO/Sem posição); (5) divider; (6) metrics row — Net Return flex:1 green/red + Daily Balance + control button; (7) MAIS INFO toggle; (8) MaisInfo expansion; (9) quick action links. Card border by state; hover translateY(-2px)+shadow. Control button: EXECUTANDO→PARAR, PARADO→INICIAR, ARQUIVADO→RESTAURAR with exact token colors. All interactive state via React state — never port dc-runtime class toggles (Pitfall 5).
    Create `frontend/src/components/robot-card/MaisInfo.tsx` — flex-wrap grid of stat mini-cards (flex:1 1 96px, --surface2, radius 10px) showing exactly 4 metrics per ROB-04: Número de trades, Trades com lucro %, Fator de lucro, Drawdown máximo; chevron rotates on expand.
    Create `frontend/src/components/robot-card/ContextMenu.tsx` — ⋮ dropdown with action set varying by robot state (ROB-05): running → Ver Sumário, Pausar/Parar, Criar backtest, Duplicar; parado → Ver Sumário, Editar parâmetros, Iniciar, Arquivar, Excluir, Criar backtest, Duplicar; arquivado → Desarquivar, Excluir. Wire Editar→/robos/{id}/parametros, Ver Sumário→/robos/{id}/sumario, Duplicar→api.duplicateRobot, Arquivar/Excluir→confirm modals with exact UI-SPEC destructive copy, Iniciar/Parar→api.startRobot/api.stopRobot. Control button on the card mirrors the same start/stop/restore handlers.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -q "MAIS INFO" src/components/robot-card/RobotCard.tsx && grep -q "PARAR" src/components/robot-card/RobotCard.tsx && grep -q "Fator de lucro" src/components/robot-card/MaisInfo.tsx && grep -qE "Desarquivar|Duplicar" src/components/robot-card/ContextMenu.tsx && grep -q "SparklineChart" src/components/robot-card/RobotCard.tsx && echo ROBOT_CARD_OK</automated>
  </verify>
  <acceptance_criteria>
    - RobotCard renders MAIS INFO toggle, state-correct control button (PARAR/INICIAR/RESTAURAR), SparklineChart (ECharts), badges
    - MaisInfo shows the four ROB-04 metrics: Número de trades, Trades com lucro %, Fator de lucro, Drawdown máximo
    - ContextMenu action set differs by state and includes Desarquivar + Duplicar
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Pixel-faithful RobotCard with MAIS INFO (4 ROB-04 metrics), state-varying context menu and control button; React-only interaction state; ECharts sparkline.</done>
</task>

<task type="auto">
  <name>Task 3: RobotList page — tabs, filters, search, grid/list, empty states, FAB</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Robot Card FAB + empty state; Segment Controls; Robot Listing copy)
    - frontend/src/lib/api.ts + frontend/src/stores/robots.ts (Task 1)
    - frontend/src/components/robot-card/RobotCard.tsx (Task 2)
  </read_first>
  <action>
    Create `frontend/src/components/robot-card/RobotFilters.tsx` — strategy multi-select dropdown ("Todas as estratégias"), "Posicionados" toggle filter, grid/list segment toggle, and name search input ("Buscar robô…"). Bind to the robots store filter state (ROB-06).
    Create `frontend/src/pages/robots/RobotList.tsx` (route /robos): three tabs EXECUTANDO/PARADOS/ARQUIVADOS (ROB-01) — PARADOS includes rascunho sub-state robots; fetch robots via TanStack Query using api.getRobots filtered by the active tab's status; render RobotCard grid or list per toggle; apply client-side strategy/posicionados/search filters (ROB-06); empty state per tab with heading "Nenhum robô neste ambiente", body, and CTA; fixed "+" FAB (UI-SPEC, position fixed bottom-right 54x54 --color-primary) plus header "CRIAR ROBÔ" button both navigating to /robos/wizard (ROB-07). Refetch list on mutation success (archive/duplicate/delete) via query invalidation. Update the sidebar robot-count badge from the executing count.
    Add the /robos/wizard route placeholder in App.tsx if not already present (the wizard screen itself is plan 07).
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -q "EXECUTANDO" src/pages/robots/RobotList.tsx && grep -q "CRIAR ROBÔ" src/pages/robots/RobotList.tsx && grep -q "Nenhum robô neste ambiente" src/pages/robots/RobotList.tsx && grep -qE "Buscar robô|Posicionados" src/components/robot-card/RobotFilters.tsx && echo ROBOT_LIST_OK</automated>
  </verify>
  <acceptance_criteria>
    - RobotList renders the three state tabs (ROB-01) with PARADOS containing rascunho robots
    - Filters component supports strategy multi-select, Posicionados, grid/list, name search (ROB-06)
    - Empty state uses copy "Nenhum robô neste ambiente"
    - CRIAR ROBÔ button + FAB navigate to the wizard route (ROB-07)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>/robos screen lists robots in three tabs with full filtering, empty states, and wizard entry.</done>
</task>

</tasks>

<verification>
- `cd frontend && npx vitest run` exits 0; `npm run build` exits 0
- SparklineChart.tsx imports echarts and uses echarts.init; container is 220x56
- /robos renders three tabs, cards with ECharts sparkline + badges + MAIS INFO (4 metrics) + context menu
- Filters, search, grid/list, empty states all functional
- CRIAR ROBÔ launches wizard route
</verification>

<success_criteria>
Robot listing complete: three state tabs (ROB-01), pixel-faithful cards with ECharts sparkline (ROB-02), accordion+menu+control (ROB-03), MAIS INFO 4 metrics (ROB-04), filters/search/toggle (ROB-06), CRIAR ROBÔ entry (ROB-07).
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-06-SUMMARY.md` when done.
</output>
