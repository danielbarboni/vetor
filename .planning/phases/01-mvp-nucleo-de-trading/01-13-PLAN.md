---
phase: 01-mvp-nucleo-de-trading
plan: 13
type: execute
wave: 4
depends_on: ["01-04", "01-06", "01-03"]
files_modified:
  - backend/routers/conta.py
  - backend/db/account_repo.py
  - backend/broker/provisioning.py
  - backend/tests/test_broker.py
  - frontend/src/pages/conta/MinhaConta.tsx
  - frontend/src/components/conta/PerfilTab.tsx
  - frontend/src/components/conta/CorretorasTab.tsx
  - frontend/src/components/conta/PreferenciasTab.tsx
  - frontend/src/components/conta/UltimosAcessosTab.tsx
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
autonomous: true
requirements: ["CTR-01", "CTR-02", "CTR-03", "CTR-04"]
must_haves:
  truths:
    - "Perfil tab edits personal data, uploads avatar, links change-password, toggles MFA placeholder (CTR-01)"
    - "Corretoras tab links/unlinks BTG/MetaAPI and shows connection status (CTR-02)"
    - "Preferências tab sets notification + display preferences (CTR-03)"
    - "Últimos Acessos tab shows login history with date, device, IP (CTR-04)"
  artifacts:
    - path: "backend/routers/conta.py"
      provides: "Account endpoints (profile, preferences, brokers, sessions/credits)"
      contains: "profile"
    - path: "backend/broker/provisioning.py"
      provides: "MetaAPI account provisioning (link broker) — stores account ID only"
      contains: "metaapi_account_id"
    - path: "frontend/src/pages/conta/MinhaConta.tsx"
      provides: "Tabbed account screen"
      min_lines: 30
  key_links:
    - from: "backend/broker/provisioning.py"
      to: "broker_connections table"
      via: "store metaapi_account_id, never raw MT5 creds"
      pattern: "metaapi_account_id"
    - from: "frontend/src/pages/conta/MinhaConta.tsx"
      to: "/account/*"
      via: "api calls per tab"
      pattern: "account|profile|preferences"
---

<objective>
Build Minha Conta (CTR-01..04): the tabbed account screen (Perfil, Corretoras, Preferências, Últimos Acessos) with backend endpoints — profile data + avatar + change password + MFA placeholder, broker link/unlink via MetaAPI provisioning (storing only the account ID, never raw credentials), preferences, and login history.

Purpose: Account management closes the MVP loop — broker linking (CTR-02) is the prerequisite for Modo Real (Phase 2) and enforces the credential-safety boundary (T-01: never store MT5 passwords). Preferences (fill policy default, decimal format) feed the engine and CSV export.
Output: A working /conta screen with four tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md

<interfaces>
From plan 03: get_current_user, supabase client, models, config (METAAPI_TOKEN).
From plan 04: backend/routers/auth.py (/account/sessions, revoke-all — login history source for CTR-04 + AUT-04).
From plan 02: profiles, broker_connections, user_preferences, user_credits tables + RLS.
From plan 06: lib/api.ts. From plan 01: UI primitives (Input, Toggle, SegmentControl, Button, Badge, Modal).

API Surface (RESEARCH): GET/PATCH /account/profile (CTR-01), GET/PATCH /account/preferences (CTR-03),
GET /account/sessions (CTR-04), GET/POST /account/brokers, DELETE /account/brokers/{id} (CTR-02),
GET /account/credits. Avatar upload via Supabase Storage; python-multipart for form data.
PRD §26.1/§26.3/§26.4/§26.5.
Security: broker linking stores ONLY metaapi_account_id after provisioning — NEVER raw MT5 password
(RESEARCH Security: MT5 credential exposure → Info Disclosure mitigation; T-01). MFA is OUT OF SCOPE
(REQUIREMENTS Out of Scope — deferred to Phase 2); render the MFA toggle as a disabled placeholder with
hint "Obrigatória para habilitar o Modo Real".
UI-SPEC Minha Conta: tab bar (.et/.et-on), Perfil grid (Dados pessoais + Segurança cards, avatar, MFA toggle),
Corretoras (ADICIONAR, ATIVA/EM BREVE status badges), Preferências (toggles + decimal segment controls),
Últimos Acessos table. Copy (UI-SPEC Minha Conta table): exact PT-BR labels for every field/section.
</interfaces>

<prototype_fidelity>
PROTOTYPE IS THE PIXEL AUTHORITY (user decision 2026-06-16). Before building Minha Conta, extract the `data-screen="conta"` region from `Plataforma Vetor v3.dc.html` (tab bar .et/.et-on, Perfil cards + avatar, Corretoras list + status badges, Preferências toggles + decimal segment controls, Últimos Acessos table) and port its markup + inline styles directly to React. Phase 1 omits the ASSINATURAS tab (Phase 2). Convert dc-runtime (`{{expr}}`→JSX, `sc-if`→conditional, `sc-for`→map, `onClick="{{h}}"`→handler, class-toggle→React state). UI-SPEC supplements exact values; on conflict the prototype wins.
</prototype_fidelity>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Account endpoints + broker provisioning (credential-safe)</name>
  <read_first>
    - PRD_Plataforma_Robos_Trading_v2.0.md §26.1, §26.3, §26.4, §26.5
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (API Surface /account/*; Security MT5 credential exposure; CTR-02 test_provision mock)
    - backend/auth/jwt_guard.py, backend/db/supabase_client.py (plan 03)
    - backend/routers/auth.py (plan 04 sessions endpoint)
  </read_first>
  <behavior>
    - GET/PATCH /account/profile reads/updates the caller's profile only
    - linking a broker stores metaapi_account_id and status, and NEVER persists a raw MT5 password (CTR-02)
    - GET /account/preferences returns defaults when none set; PATCH updates them (CTR-03)
    - GET /account/sessions returns login history rows (date, device, ip) for the caller (CTR-04)
  </behavior>
  <action>
    Create `backend/db/account_repo.py` — user-scoped access for profiles, user_preferences, broker_connections (get/update). Defaults applied when preferences row absent.
    Create `backend/broker/provisioning.py` — link_broker(user_id, broker): provisions/looks up a MetaAPI account via the MetaAPI SDK/REST and stores ONLY metaapi_account_id + status in broker_connections (status pending→provisioning→active). NEVER stores raw MT5 credentials (mitigates T-01 MT5 credential exposure). unlink_broker(user_id, id) sets status unlinked.
    Create `backend/routers/conta.py` (Depends(get_current_user)): GET/PATCH /account/profile (CTR-01, avatar_url updated after Supabase Storage upload), GET/PATCH /account/preferences (CTR-03 — default_fill_policy, decimal separators, currency, notifications), GET /account/brokers + POST /account/brokers (link) + DELETE /account/brokers/{id} (unlink) (CTR-02), GET /account/credits. Reuse /account/sessions from plan 04 for CTR-04. Wire into main.py.
    Write test_broker.py::test_provision (mock MetaAPI fixture) asserting link stores metaapi_account_id and does NOT persist any password field.
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/backend && pytest tests/test_broker.py::test_provision 2>&1 | tail -5 && grep -q "metaapi_account_id" broker/provisioning.py && grep -qiE "profile|preferences|brokers" routers/conta.py && python -c "import main" && echo CONTA_API_OK</automated>
  </verify>
  <acceptance_criteria>
    - test_broker.py::test_provision passes (stores account ID, no raw password persisted) (CTR-02)
    - profile/preferences/brokers/credits endpoints exist behind get_current_user
    - provisioning.py never writes raw MT5 credentials to broker_connections
  </acceptance_criteria>
  <done>Account + broker provisioning endpoints implemented, credential-safe, and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Minha Conta tabbed screen (Perfil, Corretoras, Preferências, Últimos Acessos)</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Minha Conta section; Minha Conta copy table; Toggle; Segment Controls; Destructive Confirmations "Desvincular corretora")
    - PRD_Plataforma_Robos_Trading_v2.0.md §26.1/§26.3/§26.4/§26.5
    - frontend/src/lib/api.ts (plan 06), frontend/src/components/ui/
  </read_first>
  <action>
    Add account helpers to `frontend/src/lib/api.ts`: getProfile, updateProfile, uploadAvatar, getPreferences, updatePreferences, getBrokers, linkBroker, unlinkBroker, getSessions.
    Create `frontend/src/components/conta/PerfilTab.tsx` (CTR-01) — Dados pessoais card (Nome completo, E-mail read-only/disabled, Telefone, CPF/CNPJ, avatar 54px + "ALTERAR FOTO" upload, SALVAR) + Segurança card ("Alterar senha" link → reset flow, MFA toggle rendered DISABLED with hint "Obrigatória para habilitar o Modo Real", "ENCERRAR SESSÕES" calling revoke-all). Exact UI-SPEC copy.
    Create `frontend/src/components/conta/CorretorasTab.tsx` (CTR-02) — "CORRETORAS VINCULADAS" list with connection status badge (ATIVA → --tint-accent; EM BREVE → outlined), "ADICIONAR"/"VINCULAR" to link BTG/MetaAPI (calls linkBroker, shows provisioning state), "DESVINCULAR" with confirm modal (exact destructive copy "Desvincular corretora?").
    Create `frontend/src/components/conta/PreferenciasTab.tsx` (CTR-03) — NOTIFICAÇÕES POR E-MAIL toggles, SIMULAÇÃO default simulator type, FORMATAÇÃO DE VALORES decimal-separator segment controls with mono format preview; persist via updatePreferences.
    Create `frontend/src/components/conta/UltimosAcessosTab.tsx` (CTR-04) — login history table (DATA/HORA, DISPOSITIVO/NAVEGADOR, IP, LOCALIZAÇÃO) from getSessions, current session badge "SESSÃO ATUAL"; IP/date in JBMono.
    Create `frontend/src/pages/conta/MinhaConta.tsx` (route /conta) — page heading "Minha conta", tab bar PERFIL/CORRETORAS/PREFERÊNCIAS/ÚLTIMOS ACESSOS (Phase 1 omits ASSINATURAS — Phase 2) using .et/.et-on, rendering the active tab. Register route in App.tsx behind RequireAuth.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -q "Minha conta" src/pages/conta/MinhaConta.tsx && grep -qE "ALTERAR FOTO|DADOS PESSOAIS" src/components/conta/PerfilTab.tsx && grep -qE "DESVINCULAR|ATIVA" src/components/conta/CorretorasTab.tsx && grep -qE "FORMATAÇÃO|SIMULAÇÃO" src/components/conta/PreferenciasTab.tsx && grep -qE "SESSÃO ATUAL|IP" src/components/conta/UltimosAcessosTab.tsx && echo CONTA_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - PerfilTab edits personal data + avatar upload + change-password + disabled MFA toggle (CTR-01)
    - CorretorasTab links/unlinks broker with status badges + Desvincular confirm (CTR-02)
    - PreferenciasTab sets notification + decimal/simulator preferences (CTR-03)
    - UltimosAcessosTab shows login history table with current-session badge (CTR-04)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Minha Conta four-tab screen complete with exact PT-BR copy and credential-safe broker linking.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/test_broker.py` exits 0
- `cd frontend && npm run build` exits 0
- All four tabs functional; broker link stores only account ID
</verification>

<success_criteria>
Minha Conta complete: Perfil with avatar + password + MFA placeholder (CTR-01), Corretoras link/unlink + status (CTR-02), Preferências (CTR-03), Últimos Acessos (CTR-04).
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-13-SUMMARY.md` when done.
</output>
