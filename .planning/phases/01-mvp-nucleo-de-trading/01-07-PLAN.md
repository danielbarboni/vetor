---
phase: 01-mvp-nucleo-de-trading
plan: 07
type: execute
wave: 3
depends_on: ["01-05", "01-01", "01-04"]
files_modified:
  - frontend/src/pages/robots/RobotWizard.tsx
  - frontend/src/components/wizard/StepIndicator.tsx
  - frontend/src/components/wizard/StepStrategy.tsx
  - frontend/src/components/wizard/StepMode.tsx
  - frontend/src/components/wizard/StepAsset.tsx
  - frontend/src/components/wizard/StepConfigure.tsx
  - frontend/src/components/wizard/SaibaMaisModal.tsx
  - frontend/src/data/strategyCatalog.ts
  - frontend/src/App.tsx
autonomous: true
requirements: ["WIZ-01", "WIZ-02", "WIZ-03", "WIZ-04", "WIZ-05", "WIZ-06"]
must_haves:
  truths:
    - "Step 1 shows a searchable strategy catalog with name, author, Saiba Mais, and select (WIZ-01)"
    - "All 7 strategy types are listed; only IT [Tangram 3.0] is selectable, others show EM BREVE disabled (WIZ-02, D-09/D-10)"
    - "Step 2 lets the user choose Modo Simulado or Modo Real (WIZ-03)"
    - "Step 3 selects market BM&F and asset chip WIN%/WDO%/BIT% (WIZ-04, D-12)"
    - "Step 4 requires a unique robot name and (Simulado) simulation capital default R$5.000,00 (WIZ-05)"
    - "On completion a robot is created in rascunho and the user is redirected to /robos/{id}/parametros (WIZ-06)"
  artifacts:
    - path: "frontend/src/pages/robots/RobotWizard.tsx"
      provides: "4-step ephemeral wizard orchestrator"
      min_lines: 50
    - path: "frontend/src/data/strategyCatalog.ts"
      provides: "7 strategy catalog entries with selectable flags"
      contains: "indicadores_tecnicos"
  key_links:
    - from: "frontend/src/pages/robots/RobotWizard.tsx"
      to: "POST /robots"
      via: "api.createRobot on finish then navigate"
      pattern: "createRobot|/robos/"
---

<objective>
Build the 4-step robot creation wizard (ESTRATÉGIA → MODO → DADOS → CONFIGURAR) as a fully ephemeral frontend session (D-13): strategy catalog with all 7 types (only IT selectable), mode selection, asset selection, and final configuration with name uniqueness + simulation capital. On finish, create the robot via POST /robots and redirect to its parameters editor.

Purpose: The wizard is the only path to create a robot (WIZ-06 → editor). It enforces D-09/D-10 (IT-only selectable), D-12 (one asset), and D-13 (ephemeral — no server state until submit).
Output: A working wizard at /robos/wizard producing a rascunho robot.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md

<interfaces>
From plan 01: UI primitives (Button, Input, Modal, SegmentControl, Badge).
From plan 06: frontend/src/lib/api.ts (api.createRobot).
From plan 04: auth store.

Decisions: D-09 all 7 strategies visible; 6 non-IT show "EM BREVE" + disabled "+". D-10 only
indicadores_tecnicos is selectable and yields an editable params screen. D-11 Saiba Mais — implement
IT modal from PRD if content exists, others minimal/placeholder (Claude discretion). D-12 one asset per robot.
D-13 ephemeral — no server call until Step 4 submit; closing browser loses progress, no cleanup.
WIZ-06 created robot is 'rascunho', redirect to /robos/{id}/parametros.

UI-SPEC Wizard: step indicator (.stc/.stc-act/.stc-done, .stl), strategy cards (.wc/.wc-on), "+" select button,
EM BREVE badge, disabled .dis (opacity 0.45 pointer-events none), mode cards (Simulado RECOMENDADO badge,
Real --tint-amber), asset chips (.lwc-on with 3px --tint-primary ring), nav buttons (VOLTAR / AVANÇAR → / CRIAR ROBÔ →).
Copy (UI-SPEC Wizard table): title "Criando seu robô", subtitle, step labels "ESTRATÉGIA"/"MODO"/"DADOS"/"CONFIGURAR",
"Buscar estratégia…", "SAIBA MAIS", "EM BREVE", "RECOMENDADO", Simulado/Real titles+descriptions,
"Nome do robô" + placeholder "Ex.: Estocástico WIN 5min" + hint "Único por conta e ambiente · [N] caracteres restantes",
"Capital para simulação" default "R$ 5.000,00", hints, "VOLTAR"/"AVANÇAR →"/"CRIAR ROBÔ →".
</interfaces>

<prototype_fidelity>
PROTOTYPE IS THE PIXEL AUTHORITY (user decision 2026-06-16). Before building the wizard, extract the `screen==='wizard'` region from `Plataforma Vetor v3.dc.html` (step indicator .stc/.stl, strategy cards .wc/.wc-on + EM BREVE, mode cards, asset chips .lwc-on, nav buttons) and port its markup + inline styles directly to React. Convert dc-runtime (`{{expr}}`→JSX, `sc-if`→conditional, `sc-for`→map, `onClick="{{h}}"`→handler, class-toggle→React state). UI-SPEC supplements exact values; on conflict the prototype wins.
</prototype_fidelity>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strategy catalog data + Step 1 (strategy) and Step 2 (mode)</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Wizard section Step 1 + Step 2; Wizard copy table)
    - .planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md (D-09, D-10, D-11)
    - PRD_Plataforma_Robos_Trading_v2.0.md §9 (wizard) and §12 (IT strategy, for Saiba Mais content)
    - frontend/src/components/ui/ (Button, Input, Modal, Badge)
  </read_first>
  <action>
    Create `frontend/src/data/strategyCatalog.ts` listing all 7 strategy types with {id, name, author, selectable}: indicadores_tecnicos ("Indicadores Técnicos [Tangram 3.0]", selectable:true) and the 6 others (Larry Williams, Tangram, Fibonacci, Toque na Média, Price Action, RenkoBot Start) with selectable:false (D-09/D-10).
    Create `frontend/src/components/wizard/StepStrategy.tsx` (WIZ-01/WIZ-02): search input "Buscar estratégia…", auto-fill grid of strategy cards (.wc); selectable card has a working "+" select button (.wc-on when chosen); non-selectable cards show "EM BREVE" badge + disabled "+" + .dis styling; each card has "SAIBA MAIS" opening SaibaMaisModal.
    Create `frontend/src/components/wizard/SaibaMaisModal.tsx` (D-11): for indicadores_tecnicos render real description sourced from PRD §12; for others render a minimal "Em breve" placeholder.
    Create `frontend/src/components/wizard/StepMode.tsx` (WIZ-03): two mode cards — Modo Simulado (RECOMENDADO badge, --tint-primary icon, description "Carteira virtual com cotações reais...") and Modo Real (--tint-amber, description "Envio de ordens reais à corretora. Requer conta vinculada..."). Selecting sets wizard.mode.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -q "indicadores_tecnicos" src/data/strategyCatalog.ts && grep -q "EM BREVE" src/components/wizard/StepStrategy.tsx && grep -q "RECOMENDADO" src/components/wizard/StepMode.tsx && echo WIZ_12_OK</automated>
  </verify>
  <acceptance_criteria>
    - strategyCatalog.ts has 7 entries; only indicadores_tecnicos has selectable:true (D-09/D-10)
    - StepStrategy shows EM BREVE + disabled select for non-IT (WIZ-02)
    - StepMode offers Simulado (RECOMENDADO) and Real (WIZ-03) with exact descriptions
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Strategy catalog + Step 1 (IT-only selectable) + Step 2 (mode) built with exact copy.</done>
</task>

<task type="auto">
  <name>Task 2: Step 3 (asset), Step 4 (configure), wizard orchestrator + completion</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Wizard Step 3/4, Step indicator, nav buttons, Wizard copy table)
    - .planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md (D-12 one asset, D-13 ephemeral, WIZ-06 redirect)
    - frontend/src/lib/api.ts (api.createRobot from plan 06)
    - frontend/src/data/strategyCatalog.ts (Task 1)
  </read_first>
  <action>
    Create `frontend/src/components/wizard/StepIndicator.tsx` — 4-step indicator (.stc/.stc-act/.stc-done + .stl labels ESTRATÉGIA/MODO/DADOS/CONFIGURAR) reflecting current step.
    Create `frontend/src/components/wizard/StepAsset.tsx` (WIZ-04, D-12): market segment BM&F label, asset chips WIN% / WDO% / BIT% (.lwc-on selected, single-select), inside the wizard card container.
    Create `frontend/src/components/wizard/StepConfigure.tsx` (WIZ-05): robot name input ("Nome do robô", placeholder "Ex.: Estocástico WIN 5min", hint "Único por conta e ambiente · [N] caracteres restantes" with live remaining count, max 40 chars), selected strategy display with "ALTERAR" link (back to Step 1), and — only when mode=simulado — "Capital para simulação" numeric input default "R$ 5.000,00" with tooltip. "AVANÇAR/CRIAR ROBÔ" enabled only when fields valid (name non-empty within length).
    Create `frontend/src/pages/robots/RobotWizard.tsx` (route /robos/wizard): orchestrates the 4 steps with ephemeral local state only (D-13 — no API calls until finish), step indicator, VOLTAR / AVANÇAR → nav, and on Step 4 "CRIAR ROBÔ →" call api.createRobot({name, strategy_type:'indicadores_tecnicos', mode, asset, simulation_capital, fill_policy:'pessimista'}). On 409 (duplicate name) show inline error on the name field (WIZ-05). On success navigate to `/robos/{id}/parametros` (WIZ-06). Register the route in App.tsx behind RequireAuth.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -q "WIN%" src/components/wizard/StepAsset.tsx && grep -q "Nome do robô" src/components/wizard/StepConfigure.tsx && grep -qE "createRobot|/robos/" src/pages/robots/RobotWizard.tsx && grep -q "parametros" src/pages/robots/RobotWizard.tsx && echo WIZ_34_OK</automated>
  </verify>
  <acceptance_criteria>
    - StepAsset offers WIN%/WDO%/BIT% single-select chips (WIZ-04, D-12)
    - StepConfigure shows name (unique hint) + simulation capital default R$ 5.000,00 only in Simulado (WIZ-05)
    - Finish calls api.createRobot and redirects to /robos/{id}/parametros (WIZ-06)
    - Wizard holds no server state until submit (D-13 — no createRobot call before Step 4 finish)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Steps 3-4 + orchestrator complete; finishing creates a rascunho robot and redirects to the editor.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` exits 0
- Wizard walks 4 steps; only IT selectable; finishing creates a robot and redirects to /robos/{id}/parametros
- No API call occurs before Step 4 submit (D-13)
</verification>

<success_criteria>
Wizard complete: searchable catalog (WIZ-01), 7 listed / IT-only selectable (WIZ-02), mode (WIZ-03), asset (WIZ-04), name+capital validation (WIZ-05), create→rascunho→editor redirect (WIZ-06).
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-07-SUMMARY.md` when done.
</output>
