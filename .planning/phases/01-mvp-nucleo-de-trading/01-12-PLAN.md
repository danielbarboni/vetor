---
phase: 01-mvp-nucleo-de-trading
plan: 12
type: execute
wave: 7
depends_on: ["01-10", "01-11", "01-08"]
files_modified:
  - backend/engine/backtest_runner.py
  - backend/routers/backtests.py
  - backend/db/backtest_repo.py
  - backend/tests/test_backtest.py
  - frontend/src/pages/backtests/BacktestList.tsx
  - frontend/src/pages/backtests/BacktestReport.tsx
  - frontend/src/components/backtests/BacktestModal.tsx
  - frontend/src/lib/api.ts
  - frontend/src/App.tsx
autonomous: true
requirements: ["BCK-01", "BCK-02", "BCK-03", "BCK-04"]
must_haves:
  truths:
    - "A backtest can be created from the editor 🧪 icon or robot card menu with capital, cost profile, type, date range + shortcuts (BCK-01)"
    - "Available backtest credits are shown read-only; each run consumes one credit (BCK-02)"
    - "The backtest list shows all runs with status, date, period, capital, type (BCK-03)"
    - "A completed backtest report shows the same metrics as Sumário + equity + read-only order list (BCK-04)"
  artifacts:
    - path: "backend/engine/backtest_runner.py"
      provides: "Event-driven backtest using StrategyBase (no vectorized lookahead)"
      contains: "on_tick"
    - path: "frontend/src/pages/backtests/BacktestReport.tsx"
      provides: "Completed backtest report reusing Sumário metric components"
      min_lines: 30
  key_links:
    - from: "backend/engine/backtest_runner.py"
      to: "backend/engine/strategy_base.py"
      via: "shares StrategyBase with live engine"
      pattern: "StrategyBase|strategy"
    - from: "frontend/src/pages/backtests/BacktestReport.tsx"
      to: "Sumário metric components"
      via: "reuses MetricCards/EquityChart/OrderList"
      pattern: "MetricCards|EquityChart"
---

<objective>
Build individual backtests (BCK-01..04): the creation modal, the credit-aware queue, the event-driven BacktestRunner (sharing StrategyBase with the live engine to guarantee parity), the backtest list with status, and the completed report reusing the Sumário metric components. Status updates flow via Supabase Realtime.

Purpose: Backtesting lets the user validate a strategy before live execution. Reusing StrategyBase + the Sumário metric components guarantees backtest metrics match live metrics (BCK-04 parity). The event-driven runner avoids the lookahead bias that corrupts vectorized backtests (Pitfall 4).
Output: Create → queue → process → view a backtest report.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md

<interfaces>
From plan 10: backend/engine/strategy_base.py, strategies/indicadores_tecnicos.py, fill_simulator.py.
From plan 11: backend/engine/metrics.py (reuse for parity), frontend Sumário components (MetricCards, EquityChart, OrderList).
From plan 02: backtests + backtest_orders tables, user_credits table, Realtime publication (backtests in it).
From plan 06: lib/api.ts; plan 08: editor 🧪 action rail icon (opens BacktestModal — EDT-04 link).

API Surface (RESEARCH): POST /backtests (create+queue), GET /backtests (list), GET /backtests/{id} (detail+results),
GET /backtests/{id}/orders, GET /account/credits (BCK-02).
PRD §21.1/§21.4: creation modal (robot name pre-filled, capital, operational costs profile, type pessimist/moderate/
optimist, start/end date with shortcuts 1M/3M/6M/1Y/2Y), credit counter, list page, completed report (same cards as Sumário).
D-? backtest distinct from Simulado (D-05): backtest is historical replay; Simulado is live paper trading.
Pitfall 4: event-driven loop (df.iloc[:i+1] per bar), entry price = next bar open — NO vectorized pandas lookahead.
RESEARCH A10: asyncio.create_task acceptable for Phase 1 solo scale; use ProcessPoolExecutor if CPU-blocking observed.
UI-SPEC Backtest Screens + copy: "Novo Backtest", period shortcuts 1M/3M/6M/1A/2A, "Tipo de simulação"
Pessimista/Moderado/Otimista, "Créditos disponíveis:", "INICIAR BACKTEST", status Aguardando/Processando/Concluído/Erro.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BacktestRunner + repo + endpoints (credit-aware queue, metric parity)</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Pitfall 4 lookahead; A10; API Surface backtests; validation test_metrics_parity)
    - PRD_Plataforma_Robos_Trading_v2.0.md §21.1, §21.4
    - backend/engine/strategy_base.py + metrics.py (plan 10/11)
    - backend/db/supabase_client.py, backtests/user_credits schema (plan 02)
  </read_first>
  <behavior>
    - creating a backtest with zero credits is rejected; with credits it queues and decrements one credit (BCK-02)
    - the runner is event-driven: a signal at bar N never uses bar N+1 data (no lookahead, Pitfall 4)
    - backtest metrics from metrics.py match the Sumário computation for the same order set (BCK-04 parity)
    - a completed run sets status='completed' and stores result_metrics + equity curve
  </behavior>
  <action>
    Create `backend/db/backtest_repo.py` — user-scoped data access for backtests + backtest_orders + user_credits: create_backtest (checks + decrements credit atomically, BCK-02), list_backtests, get_backtest (+results), get_backtest_orders, get_credits.
    Create `backend/engine/backtest_runner.py` — event-driven runner reusing StrategyBase + the IT strategy + FillSimulator: replays historical bars one at a time (iloc[:i+1], entry at next bar open — Pitfall 4), writes backtest_orders, computes results via the SHARED metrics.py (BCK-04 parity), updates status queued→processing→completed/error. Run as asyncio.create_task (A10); wrap CPU-heavy compute defensively.
    Create `backend/routers/backtests.py` (Depends(get_current_user)): POST /backtests (validate, check+consume credit, queue, launch runner — BCK-01/02), GET /backtests (BCK-03), GET /backtests/{id} (BCK-04), GET /backtests/{id}/orders, GET /account/credits (BCK-02). Status changes fan out via Supabase Realtime. Wire into main.py.
    Write test_backtest.py::test_metrics_parity (same orders → backtest metrics == Sumário metrics) + a credit-decrement test + a lookahead-guard test.
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/backend && pytest tests/test_backtest.py 2>&1 | tail -6 && grep -qi "on_tick\|StrategyBase\|strategy" engine/backtest_runner.py && grep -qE "credits|consume|decrement" db/backtest_repo.py && python -c "import main" && echo BACKTEST_API_OK</automated>
  </verify>
  <acceptance_criteria>
    - test_backtest.py::test_metrics_parity passes (BCK-04 parity)
    - create_backtest consumes exactly one credit and rejects at zero credits (BCK-02)
    - runner is event-driven (no vectorized lookahead) reusing StrategyBase + metrics.py
    - backtest endpoints exist behind get_current_user; status fans via Realtime
  </acceptance_criteria>
  <done>Backtest runner (parity + no lookahead), credit-aware repo, and endpoints implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Backtest modal + list + completed report (reuse Sumário components)</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Backtest Screens; Backtests copy table; Modals)
    - PRD_Plataforma_Robos_Trading_v2.0.md §21.1, §21.4
    - frontend/src/components/sumario/ (MetricCards, EquityChart, OrderList from plan 11)
    - frontend/src/lib/api.ts (plan 06)
  </read_first>
  <action>
    Add backtest helpers to `frontend/src/lib/api.ts`: createBacktest, getBacktests, getBacktest, getBacktestOrders, getCredits.
    Create `frontend/src/components/backtests/BacktestModal.tsx` (BCK-01) — "Novo Backtest" modal: robot name pre-filled, capital input, operational costs profile, "Tipo de simulação" segment (Pessimista/Moderado/Otimista), start/end date pickers with period shortcut chips (1M/3M/6M/1A/2A per UI-SPEC .bc/.bc-on), "Créditos disponíveis:" read-only counter from getCredits (BCK-02), "INICIAR BACKTEST" CTA → createBacktest. Opened from the editor 🧪 action rail (plan 08 EDT-04) and the robot card ⋮ "Criar backtest" (plan 06).
    Create `frontend/src/pages/backtests/BacktestList.tsx` (route /backtests, BCK-03) — list/cards of all backtests with status badge (Aguardando/Processando/Concluído/Erro), creation date, period, capital, type; subscribe to Supabase Realtime for live status transitions; empty state "Nenhum backtest criado"; error state copy. Row/card click → report.
    Create `frontend/src/pages/backtests/BacktestReport.tsx` (route /backtests/:id, BCK-04) — completed backtest report REUSING the Sumário MetricCards + EquityChart + OrderList components in read-only/historical mode (same metric cards as Sumário per BCK-04), fed by getBacktest + getBacktestOrders.
    Register routes in App.tsx behind RequireAuth.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -q "Novo Backtest" src/components/backtests/BacktestModal.tsx && grep -qE "Créditos disponíveis" src/components/backtests/BacktestModal.tsx && grep -qE "Aguardando|Processando|Concluído" src/pages/backtests/BacktestList.tsx && grep -qE "MetricCards|EquityChart" src/pages/backtests/BacktestReport.tsx && echo BACKTEST_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - BacktestModal has capital, cost profile, type segment, date range + 1M/3M/6M/1A/2A shortcuts, credits counter (BCK-01, BCK-02)
    - BacktestList shows all runs with status/date/period/capital/type and live status via Realtime (BCK-03)
    - BacktestReport reuses Sumário MetricCards/EquityChart/OrderList (BCK-04 parity)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Backtest creation modal, credit-aware list, and report (reusing Sumário components) complete.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/test_backtest.py` exits 0
- `cd frontend && npm run build` exits 0
- Create → queue → process → report flow works; metrics match Sumário
</verification>

<success_criteria>
Individual backtests complete: creation modal with full options (BCK-01), credit display+consumption (BCK-02), backtest list with status (BCK-03), completed report matching Sumário metrics (BCK-04).
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-12-SUMMARY.md` when done.
</output>
