---
phase: 01-mvp-nucleo-de-trading
plan: 11
type: execute
wave: 6
depends_on: ["01-10", "01-06"]
files_modified:
  - backend/routers/sumario.py
  - backend/engine/metrics.py
  - backend/tests/test_sumario.py
  - frontend/src/pages/sumario/RobotSumario.tsx
  - frontend/src/components/sumario/MetricCards.tsx
  - frontend/src/components/charts/EquityChart.tsx
  - frontend/src/components/sumario/RelatorioCompleto.tsx
  - frontend/src/components/sumario/OrderList.tsx
  - frontend/src/components/sumario/OrderEventModal.tsx
  - frontend/src/components/sumario/csv.ts
  - frontend/src/components/sumario/csv.test.ts
  - frontend/src/hooks/useRobotRealtime.ts
  - frontend/src/hooks/useTickStream.ts
  - frontend/src/stores/ticks.ts
autonomous: true
requirements: ["SUM-01", "SUM-02", "SUM-03", "SUM-04", "SUM-05"]
must_haves:
  truths:
    - "Sumário header shows robot name, strategy, asset, current contract, simulator badge, último salvar (SUM-01)"
    - "Metric cards + equity chart + real-time asset quote render with live updates (SUM-02)"
    - "RELATÓRIO COMPLETO accordion shows the 8 detailed metric sections (SUM-03)"
    - "Order list shows all columns, filters, pagination, and exports to CSV (SUM-04)"
    - "Order event modal shows the per-order event log (SUM-05)"
  artifacts:
    - path: "backend/engine/metrics.py"
      provides: "Sumário metric computation from orders (net return, drawdown, profit factor, equity)"
      contains: "drawdown"
    - path: "frontend/src/components/charts/EquityChart.tsx"
      provides: "ECharts 6.1.0 equity evolution line chart"
      contains: "echarts"
    - path: "frontend/src/hooks/useRobotRealtime.ts"
      provides: "Supabase Realtime subscription for status + orders"
      contains: "postgres_changes"
  key_links:
    - from: "frontend/src/hooks/useTickStream.ts"
      to: "FastAPI WS /ws/{user_id}"
      via: "single shared WS connection, multiplexed by robot_id"
      pattern: "robot_id|WebSocket"
    - from: "frontend/src/components/sumario/OrderList.tsx"
      to: "csv.ts export"
      via: "client-side Blob download with BOM"
      pattern: "csv|Blob"
---

<objective>
Build the robot Sumário report (SUM-01..05): backend metric computation + endpoints, the report screen with primary metric cards, the ECharts 6.1.0 equity line chart, the real-time asset quote (FastAPI WS), the 8-section RELATÓRIO COMPLETO accordion, the order list with filters/pagination/CSV export, and the order event modal. Wire Supabase Realtime (status/orders) and the FastAPI tick WS for live updates.

Purpose: This is how the user monitors a running robot — the second pillar of the platform's core value. It realizes the two-channel realtime model on the frontend (D-01 ticks via WS, D-02 order/status via Supabase Realtime; D-03 equity recomputed per order event).
Output: A live Sumário at /robos/{id}/sumario.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md

<!-- CHART RENDERING RULE:
     ECharts 6.1.0 is used for all charts: sparklines (robot cards), equity curves
     (Sumário/backtest), and candlestick+volume (Gráfico screen, Phase 2).
     Use echarts@6.1.0 (pinned per Assumption A1 for prototype fidelity).
     Do NOT use inline SVG for EquityChart. -->

<interfaces>
From plan 01: UI primitives, types, theme. From plan 06: lib/api.ts, SparklineChart (ECharts pattern — replicate the same echarts.init approach for EquityChart).
From plan 04: auth store (user_id, access_token for WS auth). From plan 10: orders/equity_snapshots/order_events data,
FastAPI WS /ws/{user_id} (tick envelope {robot_id, price, change, timestamp}), Supabase Realtime publication.
From plan 03: get_current_user.

EquityChart uses ECharts 6.1.0:
  props: data: Array<[timestamp: number, value: number]>, baseline?: number
  Mount via useRef + echarts.init(ref.current, null, { renderer: 'svg' })
  chart.setOption:
    xAxis: { type: 'time', show: true, splitLine: { show: false } }
    yAxis: { type: 'value', show: true }
    series: [{
      type: 'line',
      data,
      smooth: true,
      symbol: 'none',
      lineStyle: { color: 'var(--chart-1)', width: 2 },
      areaStyle: { color: 'var(--chart-1)', opacity: 0.10 }
    }]
    markLine (for baseline): if baseline provided, use series markLine at baseline value (--border2, dashed)
  Container div: width='100%', height=230 (UI-SPEC: viewBox 0 0 860 230)
  Empty state: pass data=[] — ECharts renders cleanly with no data.
  Return () => chart.dispose() in useEffect cleanup.

API Surface (RESEARCH): GET /robots/{id}/sumario (computed metrics), GET /robots/{id}/orders (paginated/filterable),
GET /robots/{id}/orders/{oid}/events, GET /robots/{id}/equity.
PRD §19: §19.1 header, §19.2 primary metric cards, §19.3 the 8 RELATÓRIO COMPLETO cards (Conta, Retorno, Risco,
Resumo dos trades, Trades com lucro, Trades com prejuízo, Trades comprados, Trades vendidos), §19.4 order list columns
+ filters + pagination, §19.5 order event modal.
D-03: equity reconstructed from order history + open-position mark; updates per order event (not tick).
UI-SPEC Sumário Report: metric cards grid (12-col), equity chart (--chart-1), order list grid columns,
ⓘ event button, RELATÓRIO COMPLETO accordion. Copy (UI-SPEC Sumário table): period filters HOJE/7D/30D/TUDO,
"EXPORTAR CSV", metric labels, relatório section labels, order columns, status/position translations.
SUM-04 CSV: client-side Blob download with UTF-8 BOM (RESEARCH test map: csv.test.ts asserts columns + BOM);
respect user decimal-separator preference (default comma).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend metric computation + sumário/orders/equity endpoints</name>
  <read_first>
    - PRD_Plataforma_Robos_Trading_v2.0.md §19.2, §19.3, §19.4 (metric definitions + order columns)
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (API Surface; D-03 equity reconstruction)
    - backend/db/writer.py + orders/equity schema (plan 10/02)
  </read_first>
  <behavior>
    - net return, profit factor, max drawdown, profitable % compute correctly from a known order fixture
    - equity series reconstructs from order history + open-position mark (D-03)
    - the 8 RELATÓRIO COMPLETO sections compute their fields from orders (§19.3)
    - order list endpoint paginates and filters by status + period
  </behavior>
  <action>
    Create `backend/engine/metrics.py` — pure functions computing from a robot's orders: net_return (R$), patrimônio (capital + result), max_drawdown (R$ and %), number_of_trades, profitable_pct, profit_factor (gross profit ÷ gross loss), daily_balance, and the equity series (D-03 reconstruction). Also compute the 8 RELATÓRIO COMPLETO sections per PRD §19.3 (Conta, Retorno, Risco, Resumo dos trades, Trades com lucro, Trades com prejuízo, Trades comprados, Trades vendidos).
    Create `backend/routers/sumario.py` (all Depends(get_current_user), user-scoped): GET /robots/{id}/sumario (header metadata + primary metrics + relatório completo sections), GET /robots/{id}/orders (paginated, filter by status + period), GET /robots/{id}/orders/{oid}/events (order_events log, SUM-05), GET /robots/{id}/equity (equity_snapshots / reconstructed series as Array<[timestamp_ms, value]>, SUM-02). Wire into main.py.
    Write test_sumario.py::test_equity_calc covering the behaviors (known fixture → expected metrics + equity series).
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/.claude/worktrees/phase-1-context/backend && pytest tests/test_sumario.py::test_equity_calc 2>&1 | tail -5 && grep -qi "drawdown" engine/metrics.py && grep -qE "sumario|orders|equity" routers/sumario.py && python -c "import main" && echo SUMARIO_API_OK</automated>
  </verify>
  <acceptance_criteria>
    - test_sumario.py::test_equity_calc passes (metrics + equity series correct)
    - metrics.py computes net return, profit factor, max drawdown, profitable %, and the 8 relatório sections
    - equity endpoint returns Array<[timestamp_ms, value]> pairs for ECharts consumption
    - sumário/orders/events/equity endpoints exist behind get_current_user
  </acceptance_criteria>
  <done>Backend metric engine + Sumário data endpoints implemented and tested.</done>
</task>

<task type="auto">
  <name>Task 2: Realtime hooks + tick stream + Sumário screen (cards, equity chart, order list, CSV, event modal)</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Sumário Report section; Sumário copy table; Top Header live quote)
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Integration Touchpoint 2 + 3; Pattern 3 Realtime; D-01/D-02/D-03/D-04)
    - frontend/src/lib/api.ts (plan 06), frontend/src/stores/auth.ts (plan 04)
    - frontend/src/components/charts/SparklineChart.tsx (plan 06 — replicate echarts.init pattern for EquityChart)
  </read_first>
  <action>
    Create `frontend/src/stores/ticks.ts` (Zustand) — live tick state keyed by robot_id. Create `frontend/src/hooks/useTickStream.ts` — opens ONE shared FastAPI WS connection per user at /ws/{user_id} (D-04), authenticates with the session token, demultiplexes by robot_id envelope into the ticks store; reconnect with backoff. Create `frontend/src/hooks/useRobotRealtime.ts` — Supabase Realtime postgres_changes subscriptions for the robots table (status, filter user_id) and orders table (filter robot_id) (Pattern 3, D-02); on order event invalidate the relevant TanStack Query so metrics + equity refetch (D-03).
    Create `frontend/src/components/charts/EquityChart.tsx` — an ECharts 6.1.0 React component. Import echarts from 'echarts'. Props: data: Array<[timestamp: number, value: number]>, baseline?: number. Use useRef + useEffect: echarts.init(ref.current, null, { renderer: 'svg' }), setOption with xAxis type='time', yAxis type='value', series type='line', smooth=true, symbol='none', lineStyle { color: 'var(--chart-1)', width: 2 }, areaStyle { color: 'var(--chart-1)', opacity: 0.10 }. If baseline is provided, add a markLine at the baseline value (dashed, --border2 color). Container div width='100%' height=230. Return () => chart.dispose() in cleanup.
    Create `frontend/src/components/sumario/MetricCards.tsx` — the 12-col metric grid: RETORNO LÍQUIDO, PATRIMÔNIO, COTAÇÃO·[contract] (live from ticks store), POSIÇÃO ATUAL, EquityChart card, and the secondary metrics (Drawdown máximo, Número de trades, Trades com lucro, Fator de lucro, Saldo diário) (SUM-02). Quote updates in real time from the WS tick stream.
    Create `frontend/src/components/sumario/RelatorioCompleto.tsx` — expandable accordion with the 8 sections (CONTA, RETORNO, RISCO, RESUMO DOS TRADES, TRADES COM LUCRO, TRADES COM PREJUÍZO, TRADES COMPRADOS, TRADES VENDIDOS) from the sumário endpoint (SUM-03).
    Create `frontend/src/components/sumario/csv.ts` — buildOrdersCsv(orders, prefs): produces a CSV string with UTF-8 BOM, the SUM-04 columns, respecting decimal-separator preference; trigger client-side Blob download (no server file). Create `frontend/src/components/sumario/csv.test.ts` (Nyquist SUM-04) asserting correct columns + BOM.
    Create `frontend/src/components/sumario/OrderList.tsx` — paginated, status+period filtered order table with the exact UI-SPEC grid columns + status/class/position translations; ⓘ button opens OrderEventModal; "EXPORTAR CSV" calls csv.ts (SUM-04). Create `frontend/src/components/sumario/OrderEventModal.tsx` — modal (640px) showing the per-order event log from the events endpoint (SUM-05).
    Create `frontend/src/pages/sumario/RobotSumario.tsx` (route /robos/:id/sumario): header (#id, simulator badge, strategy, status dot, ÚLTIMO SALVAR, current contract — SUM-01), period selector HOJE/7D/30D/TUDO, EXPORTAR CSV, MetricCards, RelatorioCompleto, OrderList; subscribe via useRobotRealtime + useTickStream for live updates.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/sumario/csv.test.ts 2>&1 | tail -5 && npm run build 2>&1 | tail -3 && grep -q "postgres_changes" src/hooks/useRobotRealtime.ts && grep -qE "robot_id|WebSocket" src/hooks/useTickStream.ts && grep -q "RELATÓRIO COMPLETO" src/components/sumario/RelatorioCompleto.tsx && grep -q "ÚLTIMO SALVAR" src/pages/sumario/RobotSumario.tsx && grep -q "echarts" src/components/charts/EquityChart.tsx && echo SUMARIO_UI_OK</automated>
  </verify>
  <acceptance_criteria>
    - csv.test.ts passes (correct columns + UTF-8 BOM) (SUM-04)
    - useTickStream opens one shared WS per user multiplexed by robot_id (D-04); useRobotRealtime uses postgres_changes (D-02)
    - RobotSumario header shows simulator badge + ÚLTIMO SALVAR + current contract (SUM-01)
    - MetricCards shows live quote from ticks; RelatorioCompleto has the 8 sections (SUM-02, SUM-03)
    - EquityChart.tsx imports echarts and uses echarts.init — ECharts line chart with xAxis type='time', smooth, areaStyle gradient (--chart-1)
    - OrderList exports CSV and opens OrderEventModal (SUM-04, SUM-05)
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Live Sumário screen with metrics, ECharts equity chart, real-time quote, relatório completo, order list+CSV, event modal.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/test_sumario.py` exits 0
- `cd frontend && npx vitest run src/components/sumario/csv.test.ts && npm run build` exits 0
- EquityChart.tsx imports echarts; uses ECharts line chart with type='time' xAxis, smooth, areaStyle
- Sumário updates live on order events (Realtime) and tick quote (WS)
</verification>

<success_criteria>
Sumário report complete: header metadata (SUM-01), live metric cards + ECharts equity chart + quote (SUM-02), 8-section relatório completo (SUM-03), order list with filters/pagination/CSV (SUM-04), order event modal (SUM-05).
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-11-SUMMARY.md` when done.
</output>
