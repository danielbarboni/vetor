---
phase: 01-mvp-nucleo-de-trading
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/index.css
  - frontend/src/App.tsx
  - frontend/src/lib/supabase.ts
  - frontend/src/lib/theme.ts
  - frontend/src/components/shell/AppShell.tsx
  - frontend/src/components/shell/Sidebar.tsx
  - frontend/src/components/shell/TopHeader.tsx
  - frontend/src/components/ui/Button.tsx
  - frontend/src/components/ui/Badge.tsx
  - frontend/src/components/ui/Toggle.tsx
  - frontend/src/components/ui/SegmentControl.tsx
  - frontend/src/components/ui/Modal.tsx
  - frontend/src/components/ui/Input.tsx
  - frontend/src/types/index.ts
  - frontend/vitest.config.ts
  - frontend/.env.example
autonomous: true
requirements: []
must_haves:
  truths:
    - "Frontend dev server starts on port 5173 with Blue Hour dark theme applied"
    - "App shell renders sidebar + top header + routed main column on every screen"
    - "Theme tokens drive all colors via data-palette/data-theme on document.documentElement"
  artifacts:
    - path: "frontend/src/main.tsx"
      provides: "Vite entry; imports tokens.css; calls initTheme()"
      contains: "initTheme"
    - path: "frontend/src/components/shell/AppShell.tsx"
      provides: "Three-layer shell (gradient + sidebar + main column)"
      min_lines: 30
    - path: "frontend/src/lib/supabase.ts"
      provides: "Single supabase client instance"
      contains: "createClient"
    - path: "frontend/src/components/ui/Button.tsx"
      provides: "Canonical button primitives (primary/secondary/destructive)"
  key_links:
    - from: "frontend/src/index.css"
      to: "blue-hour-design-system/tokens/tokens.css"
      via: "@import of generated tokens"
      pattern: "tokens.css"
    - from: "frontend/src/main.tsx"
      to: "document.documentElement"
      via: "initTheme sets data-palette + data-theme"
      pattern: "data-(palette|theme)"
---

<objective>
Establish the frontend monorepo scaffold: Vite + React 19 + TypeScript app under `/frontend`, wired to the Blue Hour Design System tokens, with the app shell (sidebar, top header, background gradient), routing stubs, the shared UI primitive components, and the Supabase JS client. This is the visual and structural foundation every later frontend plan builds on.

Purpose: Without this scaffold no frontend feature can be built. It locks in the design-system integration pattern (D-14, tokens.css import, initTheme) and the pixel-faithful shell that wraps every screen.
Output: A running React app at `localhost:5173` showing the empty app shell with Blue Hour dark theme, route stubs for `/auth`, `/robos`, `/robos/:id`, `/backtests`, `/conta`, and a library of reusable UI primitives.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md

<interfaces>
<!-- Stack versions locked by RESEARCH.md Standard Stack. Executor uses these exact versions. -->
Frontend deps: react@19.2.7, react-dom@19.2.7, vite@8.0.16, @supabase/supabase-js@2.108.2,
echarts@6.1.0 (PINNED — Assumption A1, user decision: newer stable),
zustand@5.0.14, @tanstack/react-query@5.101.0, react-router-dom@6.x, lucide-react,
tailwindcss@3.x (dev), vitest + @testing-library/react (dev).

Design system (carry forward as-is, never edit):
- blue-hour-design-system/tokens/tokens.css — relative import target from frontend
- blue-hour-design-system/palettes.js — exports initTheme(), setTheme(), getTheme()
- blue-hour-design-system/tailwind.config.js — copy to frontend/tailwind.config.js

Theme contract: <html data-palette="blue-hour" data-theme="dark"> on document.documentElement only.
CSS aliases (UI-SPEC "CSS Variable Alias Map"): --bg, --surface, --surface2, --surface3,
--border, --border2, --text, --muted, --muted2, --glow1, --glow2, --seg-on, --knob-off, --shadow.
Components use ONLY these aliases / underlying tokens — never hex values.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Vite + React + TS app and wire Blue Hour design system</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Standard Stack, Installation commands, Recommended Project Structure)
    - .planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md (D-14 monorepo, D-15 local dev, code_context reusable assets)
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Design System, CSS Variable Alias Map)
    - blue-hour-design-system/palettes.js (initTheme API)
    - blue-hour-design-system/tailwind.config.js (token→utility mapping)
  </read_first>
  <action>
    From repo root create `/frontend` and scaffold a Vite React-TS app (do NOT add a nested git repo). Install exact versions: react@19.2.7, react-dom@19.2.7, @supabase/supabase-js@2.108.2, zustand@5.0.14, @tanstack/react-query@5.101.0, react-router-dom@^6, lucide-react, echarts@6.1.0 (PINNED — A1). Dev deps: tailwindcss@^3 (install ONLY because blue-hour-design-system/tailwind.config.js requires it for token→utility mapping — NEVER use Tailwind utility classes in component code; all component styling uses inline styles + CSS custom properties only), postcss, autoprefixer, vitest, @testing-library/react, @testing-library/jest-dom, jsdom.
    Copy `blue-hour-design-system/tailwind.config.js` to `frontend/tailwind.config.js`; set content globs to `./index.html` and `./src/**/*.{ts,tsx}`. Create `frontend/postcss.config.js` with tailwindcss + autoprefixer.
    Create `frontend/src/index.css` that `@import`s the generated tokens at relative path `../../blue-hour-design-system/tokens/tokens.css`, then defines the CSS alias map from UI-SPEC ("CSS Variable Alias Map" — --bg, --surface, --surface2, --surface3, --border, --border2, --text, --muted, --muted2, --glow1, --glow2, --seg-on, --seg-on-tx, --knob-off, --shadow), the custom webkit scrollbar, the :focus-visible ring (2px --color-accent, offset 2px), antialiased smoothing on body, and the Tailwind base/components/utilities layers.
    In `frontend/src/main.tsx`: import index.css, import initTheme from `../../blue-hour-design-system/palettes.js`, call `initTheme()` so it sets `data-palette="blue-hour" data-theme="dark"` on `document.documentElement` (never on a child). Mount React root inside QueryClientProvider and BrowserRouter. Create `frontend/src/lib/theme.ts` re-exporting setTheme/getTheme for the sidebar theme toggle.
    Load Sora (600/700 ONLY — do NOT load 400 or 800 per README.md font spec), Inter (400/500/600), JetBrains Mono (400/600) via Google Fonts link in `frontend/index.html`. Note: the UI-SPEC sidebar logo uses Sora 800 but per README.md binding spec only 600 and 700 are loaded — use Sora 700 for the logo mark instead of 800.
    Create `frontend/.env.example` with VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, VITE_API_BASE_URL, VITE_WS_BASE_URL. Create `frontend/src/lib/supabase.ts` initializing ONE supabase client from those env vars (single instance, exported as `supabase`).
    Add `@/` → `./src` path alias in vite.config.ts and tsconfig.json. Create `frontend/vitest.config.ts` (jsdom environment, setup file importing @testing-library/jest-dom).
  </action>
  <verify>
    <automated>cd frontend && npm install && npm run build 2>&1 | tail -5 && test -f src/lib/supabase.ts && grep -q "initTheme" src/main.tsx && grep -q "tokens.css" src/index.css && echo SCAFFOLD_OK</automated>
  </verify>
  <acceptance_criteria>
    - `frontend/package.json` lists echarts at exactly 6.1.0 and react at 19.2.7
    - `npm run build` exits 0
    - `frontend/src/main.tsx` calls `initTheme()` (grep matches `initTheme`)
    - `frontend/src/index.css` imports tokens.css (grep matches `tokens.css`)
    - `frontend/src/lib/supabase.ts` exports a single client via `createClient`
    - `frontend/.env.example` contains VITE_SUPABASE_URL and VITE_API_BASE_URL
  </acceptance_criteria>
  <done>Vite app builds; tokens.css imported; initTheme sets theme attrs on documentElement; supabase client + env template exist.</done>
</task>

<task type="auto">
  <name>Task 2: Build the app shell (sidebar, top header, background gradient) + route stubs</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Shell Layout, Sidebar, Top Header, Scrollbar, Focus Ring sections — pixel-exact dimensions)
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (UI Fidelity Requirements → Shell + Global; Pitfall 5 — never carry dc-runtime class toggle logic)
    - frontend/src/index.css (alias map created in Task 1)
  </read_first>
  <action>
    Create `frontend/src/components/shell/AppShell.tsx` as a fixed full-viewport flex row with three z-layers per UI-SPEC "Shell Layout": (z0) a `position:absolute; inset:0; pointer-events:none` background radial-gradient overlay using the exact two-layer gradient from UI-SPEC (radial-gradient 900px 420px at 70% -10% --glow1, transparent 60% + radial-gradient 700px 420px at 8% 112% --glow2, transparent 60%); (z3) Sidebar width 236px; (z1) main column flex:1 min-width:0 containing (z2) TopHeader height 62px and a `<main>` flex:1 overflow:auto holding `<Outlet/>`. Set `font-size:14px` on the outermost shell div (not body). Apply screen-mount `fadeUp 0.25s ease` keyframe on the main content wrapper.
    Create `frontend/src/components/shell/Sidebar.tsx` per UI-SPEC Sidebar: logo mark (30x30 --color-primary, Sora 800), wordmark "VETOR" (Sora 700, letter-spacing 0.14em), subtitle "Robôs de trading", section group labels (PLATAFORMA / DESCOBRIR / PARCEIRO / ADMINISTRAÇÃO / CONTA), nav items (`.nv` class, Lucide icons 16x16 stroke-width 1.8, active `.nv-on` with inset 3px --color-primary box-shadow), robot-count nav badge (JBMono), footer with theme toggle (calls setTheme from lib/theme.ts) + user row. Active nav state MUST be React state via react-router useLocation — do NOT port dc-runtime `nv-on` toggle logic (Pitfall 5).
    Create `frontend/src/components/shell/TopHeader.tsx` per UI-SPEC Top Header: page title (Sora 700 16px), live quote widget placeholder (JBMono, pulse dot --color-accent), mode toggle (MODO SIMULADO `.ev-sim` / MODO REAL `.ev-real`). Live quote and mode toggle are static placeholders here — wired in later plans.
    In `frontend/src/App.tsx`: define routes with AppShell as layout wrapping route stubs: `/auth/*`, `/robos`, `/robos/:id/parametros`, `/robos/:id/sumario`, `/backtests`, `/conta`. Each stub renders a simple heading placeholder. Default redirect `/` → `/robos`.
  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -3 && grep -q "radial-gradient" src/components/shell/AppShell.tsx && grep -q "VETOR" src/components/shell/Sidebar.tsx && grep -qE "Outlet|Routes" src/App.tsx && echo SHELL_OK</automated>
  </verify>
  <acceptance_criteria>
    - AppShell renders gradient overlay layer (grep matches `radial-gradient`)
    - Sidebar contains wordmark "VETOR" and the 5 section group labels
    - Nav active state derived from useLocation (no `support.js` / dc-runtime imports anywhere)
    - App.tsx defines routes for /auth, /robos, /robos/:id/parametros, /robos/:id/sumario, /backtests, /conta
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Shell renders with gradient, sidebar, header on every route; route stubs reachable; active nav is React state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build canonical UI primitive components + shared types</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-UI-SPEC.md (Input Fields, Buttons, Modals, Segment Controls, Toggle Switch, Status Badge Color Map)
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Data Model — to shape shared TypeScript types)
    - frontend/src/index.css (alias map)
  </read_first>
  <behavior>
    - Button renders variant="primary" with --color-primary background and variant="destructive" with --color-loss background
    - Badge renders the correct color pair for each status from UI-SPEC Status Badge Color Map
    - Toggle calls onChange(true) when clicked from off state and reflects `.kb-on` class when on
    - SegmentControl marks the selected option with `.sg-on` and calls onChange with the option value
    - Modal renders children only when open=true and calls onClose on backdrop click
  </behavior>
  <action>
    Create `frontend/src/components/ui/` primitives, each driven by CSS aliases (no hex): Button.tsx (variants: primary --color-primary, secondary outlined --border2, destructive --color-loss/#FFFFFF; sizes compact/standard/wizard per UI-SPEC Buttons), Badge.tsx (props: status; map to UI-SPEC Status Badge Color Map — simulator Pessimista/Moderado/Otimista, EM BREVE, ATIVA, ASSINADA, robot status badges), Toggle.tsx (track 38x22, knob `.kb`/`.kb-on`, controlled value/onChange), SegmentControl.tsx (`.sg`/`.sg-on`, options + value + onChange), Modal.tsx (backdrop rgba(0,0,0,0.6), container --surface radius 16-20px, scale 0.96→1.0 + opacity 250ms --ease-out, widths 480/640, onClose on backdrop), Input.tsx (--surface2 bg, --border, radius 10px, focus --color-primary, disabled --surface3/--muted, optional mono font + help tooltip).
    Create `frontend/src/types/index.ts` with shared TypeScript types derived from RESEARCH Data Model: Robot (id, user_id, name, strategy_type, mode 'simulado'|'real', status 'rascunho'|'parado'|'executando'|'arquivado', asset 'WIN%'|'WDO%'|'BIT%', simulation_capital, fill_policy 'pessimista'|'moderado'|'otimista', params, params_saved_at), Order, OrderEvent, EquitySnapshot, Backtest, BrokerConnection, UserPreferences, Profile. These are the contracts later plans implement against.
    Write Vitest tests in colocated `*.test.tsx` files covering the five behaviors above.
  </action>
  <verify>
    <automated>cd frontend && npx vitest run src/components/ui 2>&1 | tail -8 && grep -q "export interface Robot" src/types/index.ts && echo UI_PRIMITIVES_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run src/components/ui` exits 0 with all primitive tests passing
    - `frontend/src/types/index.ts` exports interface Robot with status union 'rascunho'|'parado'|'executando'|'arquivado'
    - Button, Badge, Toggle, SegmentControl, Modal, Input all exist and use CSS aliases (no hex literals in component logic except destructive #FFFFFF per spec)
    - Modal renders children only when open and closes on backdrop click (test asserts)
  </acceptance_criteria>
  <done>UI primitives tested and exported; shared types file defines Robot/Order/Backtest contracts for downstream plans.</done>
</task>

</tasks>

<verification>
- `cd frontend && npm run build` exits 0
- `cd frontend && npx vitest run` exits 0
- App shell visible with Blue Hour dark theme; all route stubs reachable
- No imports from `support.js` or dc-runtime anywhere in `frontend/src`
</verification>

<success_criteria>
Frontend scaffold complete: Vite+React+TS builds, tokens.css drives theming via initTheme on documentElement, app shell (gradient/sidebar/header) wraps all routes, UI primitive library + shared types exist and are tested.
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-01-SUMMARY.md` when done.
</output>
