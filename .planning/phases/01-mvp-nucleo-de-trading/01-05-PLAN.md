---
phase: 01-mvp-nucleo-de-trading
plan: 05
type: execute
wave: 2
depends_on: ["01-03", "01-02"]
files_modified:
  - backend/routers/robots.py
  - backend/db/robot_repo.py
  - backend/db/models.py
  - backend/tests/test_robots.py
autonomous: true
requirements: ["ROB-07", "WIZ-05", "WIZ-06", "ROB-05"]
must_haves:
  truths:
    - "POST /robots creates a robot in 'rascunho' state and returns 201 + id (WIZ-06)"
    - "Robot name uniqueness per account is enforced (WIZ-05)"
    - "GET /robots lists the authenticated user's robots filterable by status (ROB-01 support)"
    - "Archive, unarchive, duplicate, delete endpoints exist with state-appropriate behavior (ROB-05)"
  artifacts:
    - path: "backend/routers/robots.py"
      provides: "Robot CRUD + lifecycle endpoints"
      contains: "post"
    - path: "backend/db/robot_repo.py"
      provides: "Supabase data access for robots scoped by user_id"
      min_lines: 30
  key_links:
    - from: "backend/routers/robots.py"
      to: "get_current_user"
      via: "Depends — all routes user-scoped"
      pattern: "get_current_user"
    - from: "backend/db/robot_repo.py"
      to: "robots table"
      via: "supabase queries filtered by user_id"
      pattern: "user_id"
---

<objective>
Implement the backend Robot CRUD and lifecycle API: create (wizard completion), list (filterable by status), get, update, delete, archive, unarchive, and duplicate — all scoped to the authenticated user via RLS + server-side user_id enforcement. This is the data backbone the robot list UI and wizard call.

Purpose: The wizard (WIZ-06) and robot list (ROB-01..07) need these endpoints. Duplicate (ROB-05) is also the D-08 mode-promotion path. Server-side user_id scoping mitigates T-01-04.
Output: A fully tested robots router wired into FastAPI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md
@.planning/phases/01-mvp-nucleo-de-trading/01-CONTEXT.md

<interfaces>
From backend/auth/jwt_guard.py (plan 03): get_current_user → user_id.
From backend/db/supabase_client.py (plan 03): supabase (service role).
From backend/db/models.py (plan 03): RobotCreate, RobotOut (extend as needed).

API Surface (RESEARCH): GET /robots (status filter), POST /robots, GET /robots/{id},
PATCH /robots/{id}, DELETE /robots/{id}, POST /robots/{id}/archive, /unarchive, /duplicate.
robots schema (RESEARCH Data Model): status default 'rascunho'; UNIQUE(user_id,name);
mode/asset/fill_policy/status CHECK enums.

Decisions: D-08 — mode fixed at creation; duplicate is the promotion path (copy params, user picks Real in wizard
when duplicating — duplicate endpoint copies the robot into a new 'rascunho' row, name "[orig] (cópia)").
D-12 — one asset per robot. WIZ-06 — new robot is 'rascunho', client redirects to /robos/{id}/parametros.
ROB-05 actions vary by state — backend rejects illegal transitions (e.g. cannot archive an 'executando' robot
without stopping; cannot start/stop here — that is plan 10).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Robot repository + Pydantic models (user-scoped data access)</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (Data Model robots; Pitfall 3 multi-tenancy; Security T-01-04)
    - backend/db/models.py (plan 03 base models)
    - backend/db/supabase_client.py (plan 03)
  </read_first>
  <behavior>
    - create_robot inserts with status='rascunho' and the caller's user_id, returns the new row with id
    - create_robot raises a conflict error when (user_id, name) already exists (WIZ-05)
    - list_robots returns only rows where user_id matches, optionally filtered by status
    - duplicate_robot copies params + config into a new 'rascunho' row named "[name] (cópia)"
  </behavior>
  <action>
    Extend `backend/db/models.py` with RobotCreate (name, mode, asset, simulation_capital, fill_policy, strategy_type default 'indicadores_tecnicos'), RobotUpdate (name?, params?, fill_policy?), RobotOut (full row). Validate asset ∈ {WIN%,WDO%,BIT%}, mode ∈ {simulado,real}, fill_policy ∈ {pessimista,moderado,otimista}; simulation_capital required when mode='simulado', must be null when mode='real' (D-08).
    Create `backend/db/robot_repo.py` with functions taking user_id explicitly: create_robot, get_robot, list_robots(status?), update_robot, delete_robot, archive_robot, unarchive_robot, duplicate_robot. Every query filters/sets user_id from the authenticated caller (never client-supplied). On unique-violation map to a 409 conflict (WIZ-05). duplicate_robot copies all config + params, sets status='rascunho', name suffix " (cópia)" (truncate to fit name length; UI-SPEC editor hint reserves 10 chars).
    Write pytest tests in test_robots.py covering the four behaviors (use mock/test supabase fixture from conftest).
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/.claude/worktrees/phase-1-context/backend && pytest tests/test_robots.py::test_name_unique 2>&1 | tail -4 && grep -q "user_id" db/robot_repo.py && grep -q "rascunho" db/robot_repo.py && echo ROBOT_REPO_OK</automated>
  </verify>
  <acceptance_criteria>
    - test_robots.py::test_name_unique passes (duplicate name → conflict)
    - robot_repo create sets status='rascunho' and caller user_id
    - duplicate_robot produces a new rascunho row with " (cópia)" suffix
    - RobotCreate validation enforces simulation_capital present iff mode='simulado'
  </acceptance_criteria>
  <done>User-scoped robot repository + validated models, tested for uniqueness and creation defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Robots router (CRUD + lifecycle endpoints)</name>
  <read_first>
    - .planning/phases/01-mvp-nucleo-de-trading/01-RESEARCH.md (API Surface robots endpoints)
    - backend/db/robot_repo.py (Task 1)
    - backend/main.py (plan 03 router includes)
  </read_first>
  <behavior>
    - POST /robots returns 201 with the created robot id and status 'rascunho' (WIZ-06)
    - GET /robots?status=parado returns only that user's parado robots
    - POST /robots/{id}/archive on a non-executing robot moves it to 'arquivado'; on an executing robot returns 409
    - POST /robots/{id}/duplicate returns a new robot id distinct from the source
  </behavior>
  <action>
    Create `backend/routers/robots.py` implementing the RESEARCH API Surface for robots, every route `Depends(get_current_user)`: GET /robots (status query filter), POST /robots (→201, WIZ-06), GET /robots/{id}, PATCH /robots/{id} (name/fill_policy only here — params save is plan 08), DELETE /robots/{id}, POST /robots/{id}/archive, POST /robots/{id}/unarchive, POST /robots/{id}/duplicate. Reject illegal state transitions (archive while executando → 409). Return 404 when the robot does not belong to the caller. Wire the router into main.py.
    Add test_robots.py::test_create_robot asserting 201 + id + status rascunho via httpx test client with auth_token fixture.
  </action>
  <verify>
    <automated>cd /home/dbarboni/projetos/vetor/.claude/worktrees/phase-1-context/backend && pytest tests/test_robots.py 2>&1 | tail -5 && grep -qE "duplicate|archive" routers/robots.py && python -c "import main" && echo ROBOTS_ROUTER_OK</automated>
  </verify>
  <acceptance_criteria>
    - test_robots.py::test_create_robot passes (201 + id + status rascunho)
    - All eight robots endpoints present and behind get_current_user
    - Archive of an executing robot returns 409
    - main.py imports cleanly with the robots router included
  </acceptance_criteria>
  <done>Robots router exposes full CRUD + lifecycle, user-scoped, with illegal-transition guards; tests green.</done>
</task>

</tasks>

<verification>
- `cd backend && pytest tests/test_robots.py` exits 0
- All robots endpoints reachable behind JWT guard
- user_id never taken from request body
</verification>

<success_criteria>
Robot CRUD + lifecycle API complete and tested: create→rascunho (WIZ-06), name uniqueness (WIZ-05), list/filter, archive/unarchive/duplicate/delete (ROB-05), create-robot entrypoint backing (ROB-07).
</success_criteria>

<output>
Create `.planning/phases/01-mvp-nucleo-de-trading/01-05-SUMMARY.md` when done.
</output>
