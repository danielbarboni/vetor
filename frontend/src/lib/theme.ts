/**
 * Theme helpers — thin re-export of blue-hour-design-system/palettes.js.
 * Use setTheme() to switch palette/mode; getTheme() to read current state.
 */
export { setTheme, getTheme, initTheme, PALETTES } from '../../../blue-hour-design-system/palettes.js'
